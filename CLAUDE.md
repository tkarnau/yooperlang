# Yooperlang - Claude working notes

IMPORTANT: even in Auto mode, when writing plans or example ideas, do not spend a lot of time on assumptions! Stop and ask for clarification early on contradictions or when spinning wheels on tooling or other areas to test things out.

A JS-implemented compiler for the Yooperlang language. Emits LLVM IR text, shells out to `clang` to link and produce an executable. Language spec in [SPEC.md](SPEC.md). Phases 1 through 9 plus library phases A–D have landed (per-phase docs in [plans/completed/](plans/completed/)); the full historical roadmap is at [plans/archive/roadmap.md](plans/archive/roadmap.md). **The current focus is the self-hosting bootstrap** - rewriting the JS compiler in Yoop, layer by layer, cross-checked against the JS reference. [plans/README.md](plans/README.md) is the single source of truth for what is being worked on now; the long-tail Phase 10 items live in [plans/archive/phase-10.md](plans/archive/phase-10.md).

Pipeline: source `.yoop` → **lex** → **parse** → **typecheck** → **codegen** (LLVM IR) → `clang` → executable.

when writing docs or plans or anything... **No 'Emdash'**, **no characters I can't type easily with an american keyboard, like →, or and curly quotes, etc**, **no fancy markdown tables**

## Style Guide

We don't have a style guide yet, but for new code:

- Indentation: 2 spaces (please use on all new yoop code, and convert existing code)

## Naming and file conventions

The repo currently has a mixture; this is the go-forward convention. Follow it in
new code. Existing non-conforming names are being renamed retroactively (do not
block on them, but do not add more).

- **Files and folders: `snake_case` except for modules** - modules are forced to
  match their folder path name, by convention, and should try to be single words
  so that we don't have a module namespace in code that is snake case while every
  -thing else is not.
- **Yooperlang (`.yoop`) identifiers** use one of three cases - never
  `snake_case` (reserved for files/folders; the underscore is still legal in the
  grammar but convention does not use it in identifiers):
  - **`PascalCase`**: types and type-like decls - struct / `type`, `trait`,
    `variant`, `enum`, `union`, `vtable`, type parameters - and `variant` case
    names (`Result.Ok`, `Option.None`).
  - **`camelCase`**: functions, methods, local bindings (`let` / `const`),
    parameters, struct fields, and kind names (`disposable`, `nulTerminated`).
  - **`SCREAMING_SNAKE`**: value-`enum` case names (`TokenTags.EOF`,
    `ASTNodeKind.FUNCTION_DECL`) and module-level `const` declarations
    (`SEEK_SET`, `TOKEN_SCAN_LIST`). (Note: `const` is overloaded - a top-level
    constant is `SCREAMING_SNAKE`; a local `const` binding is `camelCase`.)
- **The JS reference impl** uses ordinary JS identifier conventions (camelCase
  functions, etc.); only the file/folder `snake_case` rule applies to it.
- **Known legacy to migrate retroactively** (target in parentheses):
  - std functions are `snake_case` (`vec_new`, `read_file`, `map_get`) ->
    `camelCase` (`vecNew`, `readFile`, `mapGet`).
  - some module-level consts are `camelCase` (`tokenScanList`, `keywordList`) ->
    `SCREAMING_SNAKE` (`TOKEN_SCAN_LIST`, `KEYWORD_LIST`).
  - JS reference-impl filenames / dirs are camelCase (`jsyooplexer/`,
    `kindCheck.js`) -> `snake_case`.

## Run / test

- `npm test` - all tests (unit + e2e)
- `npm run test:unit` - fast, no clang
- `npm run test:e2e` - full pipeline, requires `clang` on PATH
- Driver entry: [src/yoopiler.js](src/yoopiler.js). End-user invocation is `yoopiler_alpha <entry.yoop>` (the `bin` name in package.json); the entry file pulls in everything else via its imports.
- `npm run build:sea` - builds a standalone binary via [scripts/build_sea.mjs](scripts/build_sea.mjs) (esbuild bundle to CJS, then Node SEA + postject). Output is `dist/yoopiler-<platform>-<arch>/` holding `bin/`, `lib/`, `samples/`, `editor/vscode/`, and generated INSTALL.md + EDITOR_SETUP.md. Host-only, no cross-compile. esbuild/postject are devDependencies; the compiler itself still has zero runtime deps.
- `yoopiler --lsp` runs the language server over stdio instead of compiling. The import of [src/lsp/server.js](src/lsp/server.js) is **dynamic on purpose** - that module attaches `process.stdin` handlers at module scope, so a static import would hijack stdin on every ordinary compile. The flag is handled before anything in `main()` reaches stdout, because in LSP mode stdout IS the protocol transport.
- `npm run package` - [scripts/package_release.mjs](scripts/package_release.mjs) wraps `build:sea`, compiles every `samples/*.yoop` with the binary it just built (a failure aborts the package - a broken sample reads as a broken install to a recipient), substitutes the `{{TARGET}}`/`{{BIN}}`/`{{PLATFORM_HUMAN}}` placeholders in `packaging/ai_setup.md` into `AI_SETUP.md`, and zips to `dist/yoopiler-alpha-<version>-<platform>-<arch>.zip`.
- Shippable extras live in [packaging/](packaging/): `samples/` (copied wholesale into the dist, so no list to maintain), `editor_setup.md`, and `ai_setup.md`. The VS Code extension resolves a compiler in three steps - the `yoopiler.binaryPath` setting, then a sibling `../../bin/yoopiler_alpha` (the dist layout, so the shipped copy is zero-config), then the repo's `src/lsp/server.js` under Node. Repo checkouts keep using the JS directly so compiler edits need no rebuild.

## Packaging and data-file resolution

The compiler reads three things off disk that aren't JavaScript: `std/` (the .yoop stdlib), `runtime/` (C sources handed to clang), and `src/astViewerTemplate.html` (`--dump-ast`). [src/install_root.js](src/install_root.js) is the single place that locates them, and it must keep working across three layouts: repo checkout, npm install (identical shape - package.json's `files` preserves it), and a packaged binary (`<prefix>/bin/yoopiler_alpha` + `<prefix>/lib/{std,runtime}`). It probes module-relative first, then executable-relative; `YOOP_STD_ROOT` / `YOOP_RUNTIME_DIR` override.

- **The runtime `.c` files can never live in a virtual filesystem.** clang is a separate process, so any bundler's snapshot fs (pkg, SEA assets) is useless for them. That's why the SEA build ships `lib/` as real files rather than embedding.
- **`import.meta.url` is undefined in the bundled build.** esbuild emits `import_meta = {}` for CJS output, so `install_root.js` guards the read and falls back to `process.execPath`. Anything new that reaches for `import.meta.url` needs the same treatment (or it should go through `install_root.js`).
- **Adding a new non-JS data file means touching three places**: `files` in package.json, the staging step in [scripts/build_sea.mjs](scripts/build_sea.mjs), and `resolveAsset`/`resolveRoot` in install_root.js. Missing the second is invisible until someone runs the binary.

## Subsystem map

### [src/jsyooplexer/](src/jsyooplexer/) - lexing (~630 lines)
- Public entry: `tokenize(src) → Token[]` at [lexer.js:406](src/jsyooplexer/lexer.js#L406). Throws on first lex error.
- Single-token form: `lexNext(src, pos)` at [lexer.js:334](src/jsyooplexer/lexer.js#L334).
- Token shape: `{ tag, start, length, intVal?, floatVal? }`. `start`+`length` are character offsets into `src` - extract text via `src.substring(start, start + length)`.
- `TokenTags` enum and `keywordTagList` map live in [lexer.js](src/jsyooplexer/lexer.js). Character predicates in [charFns.js](src/jsyooplexer/charFns.js); whitespace + nestable block comments in [charEaters.js](src/jsyooplexer/charEaters.js).
- Numeric literals support underscore separators (validated: must be between digits) and `0x`/`0b`/`0o` bases. Block comments nest. Template literals (backticks) are returned raw; `${...}` interpolation is parsed later in the parser.
- **Quote ownership**: double quote = `strLiteral`, backtick = `templateLiteral`, single quote = `charLiteral` (exclusively - there are no single-quoted strings). `lexCharLiteral` decodes one Unicode scalar (escapes `\n \r \t \0 \\ \' \"` and `\xNN`, plus astral scalars via `codePointAt`) into the token's `intVal`; empty/multi-char/bad-escape are lex errors. The parser lowers a `charLiteral` token to an `INT_LITERAL` node carrying the codepoint, so a char literal *is* an untyped int downstream - it pins to any integer type and range-checks like a numeric literal (`ch == '/'` against a `uint8`). No `char` type or AST node exists yet; `char` in [types.js](src/jsyooptypecheck/types.js) is a name placeholder, not wired into coercion.

### [src/jsyooparser/](src/jsyooparser/) - parsing (~2,000 lines)
- Public entry: `parse(src) → ASTNode` (PROGRAM root) at [parser.js:79](src/jsyooparser/parser.js#L79). Throws via `parseError` with line/column/caret; one error aborts the whole parse.
- Recursive descent with Pratt-style precedence climbing for binary ops. Precedence table at [parser.js:52](src/jsyooparser/parser.js#L52); climbing loop in `parseExpression(minPrecedence)`.
- Parser state (`pos`, `current`) is closure-scoped inside `parse()`. No resumability. Helpers: `advance`, `peek`, `peekAhead(n)` (re-lexes, no cache - keep lookahead shallow; existing max is 3 tokens for kind-prefixed bindings).
- Every node is created via `buildSourcedNode(ASTNodeKind.X)` at [parser.js:218](src/jsyooparser/parser.js#L218) so `sourceLoc` is set - downstream error reporting depends on this.
- Deferred-feature pattern: keywords reserved for later phases (e.g. `provides`, `restricts`) lex as plain identifiers and the parser emits an explicit "not yet supported" message if they appear in a clause. Mirror this when reserving anything new.
- **Loop heads**: `parseForStatement` dispatches on one token after `for` (`(` -> C-style, `IDENT in` -> for-in). The C-style init slot takes `i = expr` OR `let i = expr` / `let i: T = expr` (`node.initDeclares` + `node.initTypeAnnotation`; `const` is rejected with a message), and the step slot takes `=` or any compound op, desugaring `i += 1` into the `i = i + 1` shape the node has always carried (`ForStepCompoundOps`).
- **`..` is a binary operator at precedence 15** (looser than every arithmetic and comparison op) building a `RANGE_EXPR`, and every one built is collected onto `PROGRAM.rangeExprs` so the driver can rewrite them without an AST walk. Two things to keep in sync: the Phase 9.E slice form parses both of its bounds at `RANGE_PRECEDENCE` so `xs[i..j]` keeps its own `..` visible (inside brackets `..` is ALWAYS the slice separator, never a range), and chained bounds (`a..b..c`) are a parse error.
- **A `{` after an `IDENT.IDENT` RHS in a for-in belongs to the loop body.** `inForInIterExpr` is set while parsing the for-in RHS, and there the variant-constructor postfix only fires when the brace looks like a payload (`looksLikeVariantPayload`: `{ <name> :`, which no statement can start with). Without it `for x in self.items {` was a parse error - the workaround was prebinding the RHS to a local, which is why @derive still does that for arrays. A genuine nested payload (`for c in iterOf(Shape.Circle { r: 3 })`) still parses as one.
- **Generics (Phase 7.1)**: `parseTypeParamList()` parses `<T, U>` after a decl name and wires `typeParams` onto `FUNCTION_DECL`/`TYPE_DECL`/`TRAIT_DECL`. `parseTypeAnnotation()` accepts a type-application shape - any identifier followed by `<` becomes `{ name, typeArgs: [TypeAnnotation] }`. `>>` is split into two `gt` tokens parser-side when closing a nested type-application so `Pair<int32, Box<int32>>` parses; the lexer is unchanged. Explicit type args at call sites are not supported - `<` in expression position is still a binary op, keeping `peekAhead` shallow.

### [src/jsyooptypecheck/](src/jsyooptypecheck/) - type system (~6,400 lines across ~13 files)
- Orchestration in [typecheck.js](src/jsyooptypecheck/typecheck.js) - passes A (declarations), B (signatures), C (bodies + kind resolution).
- Types in [types.js](src/jsyooptypecheck/types.js): `PrimType`, `StructType`, `RefType`, `ArrayType`, `FuncType`, `KindType`, `KindApplication`, `TaskType`, `TraitType`, `TypeParamType`. Also `untypedInt`/`untypedFloat` literal placeholders and `error`/`namespace`.
- Per-node dispatchers (recursive on `node.kind`, not visitor): expressions in [checkExpr.js](src/jsyooptypecheck/checkExpr.js) (`resolveExprType`, `checkInitializer`); statements + function bodies in [checkStatement.js](src/jsyooptypecheck/checkStatement.js).
- Kind flow analysis (mustCall obligations, escape analysis, propagation) in [kindCheck.js](src/jsyooptypecheck/kindCheck.js).
- Coercion rules in [coerce.js](src/jsyooptypecheck/coerce.js) (`isAssignable`, `unifyArith`, literal pinning). Lexical scope chain in [scope.js](src/jsyooptypecheck/scope.js). Errors accumulated via `pushError` in [errors.js](src/jsyooptypecheck/errors.js) (not thrown). Fallible (`err`-bearing) binding tracking in [fallible.js](src/jsyooptypecheck/fallible.js). Builtin kinds (`joined`, `pooled`, `Task`) in [builtinKinds.js](src/jsyooptypecheck/builtinKinds.js). Cross-module symbol resolution in [imports.js](src/jsyooptypecheck/imports.js).
- **Generics (Phase 7.1)**: generic decls (structs/funcs/traits with `typeParams`) are registered in per-module `genericStructTable`/`genericFuncTable`/generic-trait-table - **never** in the concrete `structTable`/`funcTable`, which only ever hold monomorphic types. `TypeParamType { name, originDecl }` placeholders appear inside generic bodies; `substituteTypeParams(type, sub, instantiator)` ([types.js:464](src/jsyooptypecheck/types.js#L464)) walks types replacing them. A **type-param scope** map is threaded through the `ctx` of `resolveTypeAnnotation()` so bare `T`s in a generic body resolve as `TypeParamType`s. The **instantiation registry** ([instantiate.js](src/jsyooptypecheck/instantiate.js)) is keyed by `(declId, argTypes)` and lazily monomorphizes structs/funcs/traits on demand - every concrete `StructType`/`FuncType` derived from a generic decl is cached, frozen, and reused. **Call-site inference** for generic functions runs inside `resolveExprType` on `CALL_EXPRESSION` in [checkExpr.js](src/jsyooptypecheck/checkExpr.js): walk param/arg types in tandem, unify each `TypeParamType` against concrete arg types, error on conflicts and on un-inferrable params (no turbofish - diagnostic is the workaround). Untyped int/float literals do **not** pin a type param; they default per existing rules only if every other arg constraining the param is also untyped.

### [src/jsyoopcodegen/](src/jsyoopcodegen/) - LLVM IR emission (~3,000 lines)
- Public entries:
  - `compileEntry(absPath)` at [codegen.js:2943](src/jsyoopcodegen/codegen.js#L2943) - full pipeline from entry file.
  - `codegenProgram(modules, _moduleEnv, programState)` at [codegen.js:1533](src/jsyoopcodegen/codegen.js#L1533) - multi-module IR from already-typechecked modules; `programState.registry` carries the instantiation registry from typecheck.
  - `codegen(ast)` at [codegen.js:216](src/jsyoopcodegen/codegen.js#L216) - single-module (legacy / tests).
- Single-pass AST → LLVM IR text. No intermediate IR, no optimization. Dispatchers: `emitStatement`, `emitExpr`, `emitCall`, `emitLvalue`. Temp + label counters reset per function.
- Emits the IR into a per-invocation temp directory (`mkdtemp`, removed on exit unless `--keep-ir` is passed, which prints the path) and shells out to `clang` with link flags from [src/runtimeBuild.js](src/runtimeBuild.js). Clang is located by `resolveClang()` in [src/yoopiler.js](src/yoopiler.js): `YOOP_CLANG` wins, else `PATH`, else the Windows Program Files probe.
- Multi-module symbol mangling: `<moduleId>__<symbolName>`. Internal to codegen; doesn't appear in user source.
- **`printf` lowering lives in two near-duplicate emitters that must stay in sync**: `emitPrintfCall` ([codegen.js](src/jsyoopcodegen/codegen.js)) and `emitPrintfCallInner` (the multi-module path). Contract: a call with an explicit format-string **literal** is C `printf` - the literal's `%` directives are authoritative and trailing value args fill them, so bare value args do **not** get an auto-appended specifier. A specifier is only synthesized for template-literal interpolations and for a bare value arg when no format literal is present (`printf(someString)` -> `%s`). Editing one emitter without the other reintroduces the doubled-directive bug (`printf("x=%d\n", x)` emitting format `"x=%d\n%d"`).
- **Generics (Phase 7.1)**: monomorphic struct/function definitions are emitted from the typechecker's instantiation registry, *not* by walking AST `TYPE_DECL`/`FUNCTION_DECL` nodes for generic decls. The registry walk in `codegenProgram` emits one LLVM struct per resolved `StructType` and one LLVM function per `FuncType` instance, mangled as `<moduleId>__<name>__<arg1>__<arg2>` (nested args flatten via the same `__` scheme). `structContainsTypeParam` ([codegen.js:1642](src/jsyoopcodegen/codegen.js#L1642)) is the gate that skips emitting "open" (still-parameterized) types. Generic function bodies are emitted by cloning the AST and pre-substituting type-param-bearing fields via `cloneAstWithSubstitution` ([codegen.js:1605](src/jsyoopcodegen/codegen.js#L1605)) - codegen never sees a `TypeParamType` directly.
- **DWARF ([debugInfo.js](src/jsyoopcodegen/debugInfo.js))**: one `DebugInfo` per program; `beginModule` per module, `subprogram` per `define`, `location` per statement, `finalize()` appended to the IR. `typeRef(yoopType)` is the type side - it describes **the layout codegen actually emits** (mirror `llvmType`, not the source-level idea of the type): struct -> `DW_TAG_structure_type` with per-field offsets from the injected `sizeOfType`/`sizeOfAlign`; `string` -> typedef over `char *` (that typedef is what gets a debugger's C-string summary); `T[]` -> the `{ data, len }` fat pointer with `data` typed as `T *`; `variant` -> `{ tag, payload }` where tag is a `DW_TAG_enumeration_type` over the case ordinals and payload is a `DW_TAG_union_type` of the per-case structs; `union` -> a real DWARF union (codegen's `[N x i8]` is an implementation detail); `enum` -> enumeration; `vtable` -> `{ ctx, ...method slots }`. Nominal aggregates **reserve their `!N` before building members** so `type Node { next: unsafe_ptr<Node> }` terminates; everything is cached by a structural key so one node is shared program-wide.
- **Do not attach `!dbg` to the parameter alloca/store prologue.** LLVM derives the DWARF `prologue_end` marker from the first non-meta instruction carrying a location. If the `store %<p>.arg -> %<p>` lines have one, a *function* breakpoint (`b <name>`, which is what VS Code function breakpoints and lldb's default resolution use) stops BEFORE the arguments reach their stack slots and every parameter reads as garbage. `finalizeFnDbg` takes a `prologueEnd` index and sweeps only past it - calls inside the prologue region still get a location, because the verifier requires one on every inlinable call site.

### [runtime/](runtime/) - the C runtime (~2,600 lines)

Linked into every compiled program; the source list codegen hands clang lives in [src/runtimeBuild.js](src/runtimeBuild.js) (**adding a `.c` file means adding it there too**, plus the mirror list in [runtime/tests/run_tests.sh](runtime/tests/run_tests.sh)). Contract doc: [plans/runtime-design.md](plans/runtime-design.md) (written against the 6.3 MVP - read its scope lists with the "since landed" note at the top).

- [yoop_platform.h](runtime/yoop_platform.h) - the platform shims (`yoop_mutex_t` / `yoop_cond_t` / `yoop_thread_t` plus their inline ops), shared by every TU. **`yoop_cv_wait_until_locked` is the one timed wait in the runtime** - Linux gets `CLOCK_MONOTONIC` condattrs + an absolute timespec, macOS uses `pthread_cond_timedwait_relative_np` (no `pthread_condattr_setclock` there), Windows keeps relative ms. Anything that blocks with a deadline goes through it, which is what stops the deadline clock and the condvar clock from drifting apart.
- [yoop_runtime.c](runtime/yoop_runtime.c) - worker pool, task submit/wait, pooled refcounts, park tokens, timers, errno bridge. **`yoop_now_ns` is monotonic on all three platforms** and is the base for every deadline; `yoop_wall_ns` is the separate wall-clock reading and must never be used as a deadline base.
- [yoop_cancel.c](runtime/yoop_cancel.c) - cancellation tokens: a cancelled flag, an optional deadline, a waiter list, and parent/child links. **There is deliberately no timer thread** - a deadline fires because every blocking call folds the token's deadline into its own timed wait via `yoop_cancel_effective_deadline`. The cascade in `yoop_cancel_request` snapshots children under the parent's lock (with a try-retain that skips a child already being destroyed) and then drops the lock before touching any of them, so a deep chain never holds two token mutexes at once.
- [yoop_io.c](runtime/yoop_io.c) - the kqueue/epoll multiplexer, plus assorted stat/dirent helpers. See the invariant below.
- Others: `yoop_net.c` (socket constant helpers), `yoop_alloc.c` (arena + per-thread allocator), `yoop_debug.c`, `yoop_format.c`, `yoop_args.c`.
- C-level tests in [runtime/tests/](runtime/tests/), run both by `sh runtime/tests/run_tests.sh` and from [src/runtimeC.test.js](src/runtimeC.test.js) (so `npm test` covers them). A new test needs adding to **both** lists.

### [src/jsyoopdriver/](src/jsyoopdriver/) - driver (~80 lines)
- [moduleGraph.js](src/jsyoopdriver/moduleGraph.js) walks imports from the entry file, detects cycles, returns a topologically ordered list of modules for typecheck + codegen. Autoloads std/core/format.yoop + strings.yoop (template-literal lowering) and std/core/traits.yoop (@derive) into every graph, BEFORE the entry walk - the order is load-bearing: a deriving module's pass C validates its grafted to_string against Display's method table, which is only filled once the traits module's own pass C has run.
- [moduleId.js](src/jsyoopdriver/moduleId.js) - stable per-module id derivation (used for mangling).
- [lower_range.js](src/jsyoopdriver/lower_range.js) - the ONLY stage that knows `RANGE_EXPR` exists. Runs inside `loadOne` right after `parse`, rewriting every `a..b` in place into `$range.exclusive(a, b)` and unshifting `import * as $range from "std/core/range.yoop"` onto the body. It runs BEFORE the import walk, so the synthesized import resolves through the ordinary graph machinery (which is also what pulls range.yoop in, and only for modules that use `..`). `$range` is unspellable in user source, so it can't collide - same trick as the parser's `$region$N`. Consequence: typecheck and codegen have no range rules at all, and `Range` stays a plain userland type. The legacy single-module path (`compileSource`) does not lower ranges, like it doesn't do template literals.
- [test_mode.js](src/jsyoopdriver/test_mode.js) - `--test` discovery + synthetic entry generation. See "Testing yoop code" below.

### [src/jsyoopderive/](src/jsyoopderive/) - @derive expansion (Phase 13.C / 13.D, ~400 lines)
- `expandDerives(modules, errors)` runs at the FRONT of `typecheckProgram` (before pass A). `@derive(display)` on a struct or variant decl generates the `Display.to_string` method AS SOURCE TEXT from the decl's field annotations, reparses it with the ordinary parser inside a dummy wrapper type, restamps every sourceLoc onto the decl (diagnostics never point into synthetic text), grafts the METHOD_DECL, appends `implements Display`, and consumes the ATTRIBUTE wrapper (codegen and the comptime pass do NOT unwrap ATTRIBUTE - only typecheck's `innerDecl` does). Downstream passes fill resolvedFuncType/resolvedType as if hand-written.
- Field coverage: prims + named Display-implementing types interpolate (`${self.f}`, refs deref); `T[]` and `Vec<T>` of such elements get generated loops (arrays prebind to a local first - `for x in self.f {` trips the variant-constructor postfix `IDENT.IDENT {`); Map / fn types / other generics print fixed placeholders (`<map>`, `<fn>`), never an error. Rejected with accumulated errors: generic decls, type aliases, a pre-existing manual `to_string`. A deriving module that doesn't bind `Display` gets a synthesized `import { Display } from "std/core/traits.yoop"` unshifted onto its body (imports-first rule).
- **Phase 13.D - variants (`@derive(display) variant Shape { ... }`)**: the body is an arm-per-case `switch (self)` (exhaustive, so no trailing return), payload fields bound to generated `_deriveP<case>_<field>` locals and formatted by the same per-field rules. Output mirrors constructor syntax (`Shape.Circle { r: 5 }` / `Shape.Dot`) so a dump reads back as the source that would rebuild it. **Design decision: per-case control is composition, never a hand-written method** - declare the payload type OUTSIDE the variant with its own `Display` impl and the derived arm's `${payload}` dispatches to it. That is deliberate: it keeps variants free of hand-written methods, which is what lets the bootstrap skip generic-variant method substitution entirely (see the deferred item below).
- **Deferred**: `@derive(display)` on a GENERIC variant/struct. Generic variants would additionally need `instantiateVariant` to substitute methods/implementsTraits and a codegen `emitGenericVariantMethods` analog - neither exists (13.B was monomorphic-only). Also deferred: recursive inline expansion of a non-Display field type (printing `Plain { w: 9 }` without `Plain` deriving), which would need the generator to resolve type NAMES to decls pre-typecheck; today it is a clear error instead.
- **Derived-body diagnostics**: a non-printable field would otherwise surface the raw "template literal interpolation must be..." wording for a template the user never wrote. `annotateDerived` tags each generated TEMPLATE_LITERAL with `deriveOwner` + `deriveLabels` (generated-local -> field name), and `resolveTemplateLiteral` uses them to emit `@derive(display) on "X" cannot print field "f" of type T - add @derive(display) to that type, ...`.
- Registry entry in [registry.js](src/jsyoopattributes/registry.js) does parse-time shape validation (one ident arg; only `display` today - `eq`/`clone`/`hash`/`debug`/`default` are reserved with a deferred-feature message) plus a comptimePhase safety net for statement-position derives the expansion never scans.

### [std/http/](std/http/) - the HTTP layer (~1,800 lines of yoop)

Rewritten in the std/http rework ([plans/completed/std-http-rework.md](plans/completed/std-http-rework.md)); read that first for the why.

**All seven files are ONE module** (each declares `module http;`) - see the directory-modules invariant below. Consequences when editing here: the files do NOT import each other, every declaration is visible across all seven, symbols mangle as `http_<hash>__<name>`, and **[wire.yoop](std/http/wire.yoop) exports nothing at all** (it used to export its whole surface just so its three consumers could import it). File roles and the invariants that cross them:

- [types.yoop](std/http/types.yoop) - the vocabulary: `HttpMethod`, `StatusCode` (implements `Display`), `Headers`, `Params`, `Request`, `Response`, `ResponseBody`, and `HttpError`. **`HttpError` carries the status the client should see**, which is what lets a failure detected deep in the parse arrive as a 400 instead of a blanket 500; it implements `WithContext<HttpError>` so `expr? "ctx"` stacks context without flattening the status away.
- [wire.yoop](std/http/wire.yoop) - INTERNAL. Message building (`pushStr`, `pushHeaderLine`) and the one shared header-field reader. Server and client both go through it, which is what keeps the two directions from drifting. **`readHeaderLines` scans to `headEnd + 2`**: `findHeadEnd` points at the CR terminating the LAST header line, so a scan bounded by it silently drops that field (which is exactly what the pre-rewrite parser did).
- [url.yoop](std/http/url.yoop) - target splitting, percent coding, urlencoded name/value parsing (query strings and form bodies share a grammar). **Split first, decode after** - and `decodePath` rejects an encoded separator (`%2F`, `%5C`) rather than decoding it, so `req.path` stays one safely-routable field.
- [parser.yoop](std/http/parser.yoop) - `parseRequestHead` / `parseResponseHead`, both `Result`-returning and both socket-free (they parse a byte buffer, report where the body starts, and let the caller read it).
- [server.yoop](std/http/server.yoop) - `Handler` trait, the `Dispatcher` vtable, `ServerConfig`, and the accept loop. **The loop is not generic** - it takes the erased `Dispatcher`, so there is one copy in the binary and the per-connection work could be split into named functions. **A bad client costs its own connection and nothing else**; only a listener failure ends `serve`. The server owns framing (`Content-Length` / `Connection` / `Transfer-Encoding` are written here and skipped from the handler's list). `Dispatcher.from(ref h)` stores a POINTER to `h`, so handlers must outlive the route table, and it needs a concrete struct receiver (a type parameter is rejected), which is why there is no generic `serveN` wrapper.
- [router.yoop](std/http/router.yoop) - `matchPath` (literal segments, `:name` captures, a trailing `*`), first-registered-wins, 405-with-`Allow` vs 404, HEAD falling through to GET. The installed fallback renders both the 404 and the 405 with `resp.status` pre-set.
- [client.yoop](std/http/client.yoop) - `Client` + `send` / `get` / `post` / `put` / `delete`, `Result<ClientResponse, HttpError>`. Bounded by `maxResponseBytes`; no TLS, no redirects, no pooling.

Consumers: [examples/pass/hello_server](examples/pass/hello_server), [http_router](examples/pass/http_router), [http_client_loopback](examples/pass/http_client_loopback), [http_parse_smoke](examples/pass/http_parse_smoke), [http_url_smoke](examples/pass/http_url_smoke), and the CRUD demo at [examples/playground/todo_api](examples/playground/todo_api).

### [modules/](modules/) and [tools/yoopdist/](tools/yoopdist/) - modules outside std

- **[modules/](modules/) is a program-owned import root, and the repo root is one.** `modules/<name>` resolves by walking up from the IMPORTING file to the nearest `modules` directory, then through the same tail the `std/` branch runs (so a directory module, a module under a grouping directory, and a single `.yoop` file all work with no code of their own). Anchoring on the importing file rather than the entry point is what lets one import line resolve against a sibling `modules/` while a module is being developed and against the consumer's flat `modules/` once installed, with **no rewriting**. Design + as-built: [plans/modules-folder.md](plans/modules-folder.md).
- **Dependencies are FLAT and that is enforced.** A module directory carrying its own `modules/` directory is a hard error. Two copies of one module would LINK fine (`moduleId` hashes the path, so the symbols differ) and then fail as two distinct nominal types - `Value` is not assignable to `Value`. The error names both directories rather than leaving that to be discovered.
- **This root is program-relative and must never become a candidate prefix in [install_root.js](src/install_root.js)** - `std/` and `runtime/` ship with the compiler, `modules/` belongs to whoever is being compiled.
- **A module's tests ship inside its directory.** `*.test.yoop` is excluded from a directory module's source list, so the test file is its own module and imports the module under test by the same `modules/<name>` path a consumer writes. Worked example: [examples/modules_demo/](examples/modules_demo/).
- **[tools/yoopdist/](tools/yoopdist/) is written in Yoop** and builds the directory a user installs: copies sources + tests, skips `modules/`, regenerates the `requires` block of the advisory `MODULE` file. The author owns `name`/`version`; the tool owns the dependency snapshot and **preserves a version it cannot verify** rather than downgrading it to `unknown`. Nothing in the compiler reads `MODULE`.
- `MODULE` is extensionless, so [.gitignore](.gitignore) needs an explicit `!MODULE` (same as `!LICENSE`) or the blanket `*` rule drops it.

## Shared contracts

- [src/contracts.js](src/contracts.js) - `ASTNodeKind` enum (40+ kinds) and `ASTNode` constructor. Every AST consumer depends on these names; renaming a kind is a whole-pipeline change.
- [src/helpers.js](src/helpers.js) - `posToSourceLocation` and related utilities.
- [src/runtimeBuild.js](src/runtimeBuild.js) - path to the C runtime (under [runtime/](runtime/)) and the clang link flags codegen needs.

## Cross-cutting invariants

The things that aren't obvious from reading any single file - read this section before editing across stages.

- **A module is a single file OR a DIRECTORY of files that each declare `module <name>;`.** Design + as-built: [plans/modules-as-directories.md](plans/modules-as-directories.md). Directory modules today: [std/core/cancel/](std/core/cancel/), [std/db/sqlite/](std/db/sqlite/), [std/net/](std/net/), [std/http/](std/http/). Everything else is still one-file-one-module, including all of [std/core/](std/core/) and [std/collections/](std/collections/) (deliberately - those are directories OF modules, the way Odin's `core:` is a collection rather than a package).
  - **A SOURCE FILE stays the compilation unit; only the namespace moves to the directory.** Each file keeps its own AST/src/absPath, so diagnostics, DWARF compile units and the LSP are unchanged. What is shared is `moduleEnv` (keyed by `mod.id`) and the mangling prefix. Identity is the resolved DIRECTORY path; the declared name is only a label, so two directories may declare the same name and stay distinct modules.
  - **Declarations are shared; IMPORTS are not.** Siblings see each other's decls (exported or not, which is the whole point) but using a name only a sibling imported is an error - pass B.1, [importLocality.js](src/jsyooptypecheck/importLocality.js). That is an enforcement pass, not lexical per-file scope; the doc says why.
  - **`import.unsafe;` / `import.test;` stay PER SOURCE FILE.** That is what lets [std/core/cancel/ffi.yoop](std/core/cancel/ffi.yoop) name `unsafe_ptr` while its sibling token.yoop may not.
  - **Importing one source file of a directory module is an error** - the module is the unit, so `import ... from "std/http"`, never `"std/http/server.yoop"`. A mixed directory (a file missing the header) is an error too.
  - **Anything named `<moduleId>_<per-invocation counter>` in codegen needs the `fileTag` suffix**, because `codegenWithModuleId` runs once per SOURCE FILE. Two symbol families already had to be fixed for this (the module-init function and the string/array literal globals); a third, `emittedFromFnShims`, had to move to `programState` for the same reason. If you add a module-scoped generated symbol, suffix it or share its counter.
  - **A per-MODULE index built inside a per-FILE loop must MERGE, and both directions of getting this wrong fail silently.** The loop body sees one source file; the map is keyed by `mod.id`, which siblings share. Overwriting drops every sibling's entry, and consulting a file-local set answers "no" for a name a sibling declared. Three separate bugs, none of which any earlier stage caught: `funcDeclsByModule` in typecheck pass D (rebuilt per file, so **a `cleared T` sink in a sibling file stopped being enforced** - kindFlow's `crossModuleCallee` returned null and the check just did not run, with no error and no warning); codegen's `externFnNames`/`cExportNames` (built per file, so a sibling's call to an `extern "C"` symbol emitted a `<moduleId>__`-mangled name that nothing defines - **through typecheck AND IR generation**, caught only when clang read the IR); and `variantDeclsByModule`, which merges and says why. `programState.moduleExternFnNames` / `moduleCExportNames` are collected module-wide in `codegenProgram` for this reason; the per-file `declare` lines stay per-file.
  - **Every name codegen generates for itself is a name a user binding can collide with**, because LLVM puts local values and basic-block labels in ONE namespace. `isEmitterReservedSlot` ([codegen.js](src/jsyoopcodegen/codegen.js)) is the list - `%tN` temps, `__ret`, and `<freshLabel hint>_<N>` for every hint spellable as an identifier - and `createLocalSymbols.declare` renames a colliding binding to `<name>.1`. `t0`/`t1` are the ones that actually bite (natural names for timing code) and the symptom is an LLVM verifier error a hundred lines from the source. Names built with a `.` (`%p.arg`, `coro.body`, `await.done`) need no entry: `.` is not legal in a yoop identifier. **Adding a `freshLabel` hint means adding it to `EMITTER_LABEL_HINTS`.**
  - **Pass C is GROUP-MAJOR, STAGE-MINOR** for the same reason: files of one module have no declaration order between them, so every stage completes for all of a module's files before the next stage starts. Making pass C stage-major across ALL modules instead does NOT work (trait signature resolution instantiates generics, and instantiation snapshots the generic decl).
  - **`mod.siblings` is how anything holding one Module reaches the whole namespace.** A `Map<moduleId, Module>` (the LSP's `modById`, and any similar index) keeps only ONE file per module, so a name-by-name lookup against `module.ast` silently misses whatever a sibling declares. Every such lookup in the LSP goes through `findInModule` in [nav.js](src/lsp/nav.js) instead - that applies in BOTH directions, to the file under the cursor and to an imported directory module. `references.js` and `completion.js` had their own private copies of the file-only helper; those are gone.
  - **`module` is a contextual keyword, not a reserved word.** The parser recognizes `IDENT IDENT ;` only as the first item of a file, so `module` stays usable as a field or binding name. The VS Code grammar mirrors that with a column-0 anchored rule; [grammar.test.js](src/lsp/grammar.test.js) guards it, because a plain `\b(module)\b` would miscolor every field named `module` and nothing else would catch it (semanticTokens.js declares a `keyword` type but never emits one - keyword coloring is grammar-only).

- **`async` is a real color, and both halves of the rule are load-bearing.** An `async` function lowers to an LLVM switched-resume coroutine; `await` is the only way to call one, and `await` is only legal inside another async function or a `task` body (implicitly async). Together those two rules guarantee a suspend always has a coroutine frame to propagate into - which is the entire reason the LANGUAGE has to know about async rather than the runtime handling it alone (`llvm.coro.suspend` suspends one frame, and the blocking call is always several frames down). Enforcement is `resolveAwaitExpression` + `noteAsyncCallee` in [checkExpr.js](src/jsyooptypecheck/checkExpr.js). **`isAsync` is part of `FuncType` and part of `sigsEqual`**, so a sync impl of an async trait method is a mismatch - and it must be threaded through every helper that rebuilds a signature (`substituteSelfInSig`, `substituteSelfPlaceholder`, and the legacy single-module path, all of which silently dropped it at first). **A `task` call site is a SPAWN, not an await** (it evaluates to `Task<T>`); the carve-out keys on the callee's declared return being a TaskType, without which every `pooled h = f()` reports as an un-awaited async call. Design + as-built notes: [plans/async-coroutines.md](plans/async-coroutines.md).
- **The async ABI: `define ptr @f(params..., ptr %__ret) presplitcoroutine`.** An async function RETURNS its coroutine handle and writes its declared result through a caller-owned slot, so the slot lives in the caller's frame and survives the caller's own suspends. Void async functions still take the (unused) slot - one ABI, not two. There is **no initial suspend**: calling an async function runs it eagerly to its first real suspend point, so a call that never blocks costs one frame allocation and no resume. In codegen, `currentCoro` is non-null while emitting an async body and carries the shared trailer labels; **`return` inside a coroutine stores to `%__ret` and branches to the final-suspend block** rather than emitting `ret`. The entry block deliberately ends with `br label %coro.body` so `hoistAllocasToEntry` lands allocas AFTER `coro.begin`, which is where the coro passes expect frame-resident allocas.
- **The C runtime must never link against the coroutine trampolines by name.** `coro.resume`/`destroy`/`done` are LLVM intrinsics that C cannot call, so codegen emits three ordinary wrapper functions - but the runtime receives them as **function pointers** via `yoop_runtime_set_coro_ops` (installed by `main`), because `runtime/*.c` has to keep building standalone for [runtime/tests/](runtime/tests/). With no ops installed a task is treated as finishing in one step (the pre-async behavior). The trampolines are emitted **once per program with external linkage**: every module is concatenated into one `.ll` so per-module copies are a redefinition, and `linkonce_odr` gets them deleted by globaldce since nothing inside the IR ever calls them.
- **The task struct's runtime-owned prefix now runs to offset 48: coro handle at 32, allocator context at 40, result slot at 48.** Each was appended rather than inserted, on purpose - every offset the C runtime hard-codes is below the new one and stays valid. The thunk hands the body the task's OWN result slot as its `__ret`, so a finished coroutine has already written its result where `wait` looks, with no copy. `yoop_task_settle` is the single place "finished vs. suspended" is decided, and it runs after both the initial start and every resume. **Moving the result slot means touching FIVE codegen sites, and two of them are byte offsets rather than struct indices** (`getelementptr inbounds i8, ptr %h, i64 48` in the anonymous-`wait` and `wait_until` paths) - a grep for the struct index alone misses them, and the symptom is every task result reading back as zero. **The fake handles in [runtime/tests/](runtime/tests/) mirror this layout and must be updated with it**; they carry `_Static_assert`s on every offset now, because when they drifted (they were still declaring `result` at 32) the runtime wrote through the coro and context slots past the end of the struct and the tests segfaulted rather than reporting a mismatch.
- **The ambient allocator context is per-THREAD, except while a task step is running, when it is per-TASK.** `run_task_step` ([yoop_runtime.c](runtime/yoop_runtime.c)) saves the worker's own context, installs the task's from the handle's offset-40 slot, and writes back whatever the step left installed (discarding it instead when the step finished the task). Without that swap the ambient allocator stays on whichever worker ran the last step, and all three of these are silent: a parked task's arena is handed to the next unrelated task that worker picks up, a task resuming on a different worker allocates outside its own region, and its eventual `popAllocator` writes one worker's context onto another's. **This is not an async-only concern** - `yoop_task_wait` drains the queue re-entrantly on the calling thread, so a plain synchronous function holding an arena would otherwise run an arbitrary task inside its region. A spawned task deliberately inherits **nothing** from its spawner (an arena is a single-threaded bump pointer whose lifetime is its scope, so sharing it and outliving it are both unsound); the record is allocated lazily - on the suspend path, or on first temp use - so a task that neither suspends nor touches scratch never allocates one. **The temp arena rides the same record and follows the same rule** (`yoop_temp_handle` / `yoop_temp_reset`): a task's scratch is created on first use, destroyed with the task, and `resetTemp()` inside a task NEVER falls back to the thread's arena, because that would clear storage belonging to whoever dispatched it. Non-task threads keep a per-thread arena, unchanged. Design + as-built: [plans/async-allocator-context.md](plans/async-allocator-context.md). Fixtures: [async_arena_context.yoop](examples/pass/async_arena_context.yoop), [arena_sync_wait.yoop](examples/pass/arena_sync_wait.yoop), [task_temp_isolation.yoop](examples/pass/task_temp_isolation.yoop).
- **Two of those fixtures depend on a blocking `hog` task pinned to one worker, and that is load-bearing.** `arena_sync_wait` and `task_temp_isolation` need main to be the thread that runs the task under test, so they occupy the single worker with a task doing `conc.sleep_ms` (a BLOCKING call, deliberately not an `await`, which would hand the worker straight back) and let `yoop_task_wait`'s re-entrant queue drain do the dispatch. Without that the task lands on a worker whose thread-local state is already distinct from main's, and both fixtures pass against a broken build.
- **`wait` BLOCKS a thread; `conc.awaitTask(h)` suspends a task. Inside a task body only the second is legal.** `wait` lowers to `yoop_task_wait`, which parks the calling thread (Phase 9.I's "suspendable wait" is re-entrant queue DRAINING, not coroutine suspension), so the typechecker rejects `wait` in a task body outright - a task joining another task that way would hold its worker while the target sat unstarted in the queue. `awaitTask` arms a completion interest, suspends, and resumes when the target finishes; **"await all" is an ordinary loop over it**, since each iteration frees the worker and completion order does not change the answer. Underneath: a `{target, waiter}` registry under `queue_mu` fired from `yoop_handle_signal_done` (which already holds that lock, so arming is provably ordered against completion), and **a fired waiter is disarmed COMPLETELY, not just for the target that fired** - one wake is all a task needs, and that rule is what makes a future first-of-N join correct. `armComplete` / `isDone` are intrinsics rather than externs because they take a `Task<T>`, which an `extern "C"` signature cannot name. Design, plus four pre-existing bugs found and the reason `TaskGroup`/`awaitAll`/`awaitRace` are deferred: [plans/task-combinators.md](plans/task-combinators.md).
- **A wake can arrive before the task it targets has actually suspended, and the park byte at handle offset 10 is what makes that safe.** An interest is armed from INSIDE a running task (`awaitReadable` registers and only suspends several frames later; `awaitTask` likewise), so the event can fire while the task is still executing on its worker. Queuing the handle there let a second worker call `coro.resume` on a coroutine mid-execution on the first - a data race on the frame, not a lost wakeup. `yoop_task_make_runnable` therefore queues only from PARKED and otherwise records a WAKE that `run_task_step` consumes at the end of the step. Same three-state shape as the thread park token in the same file. This was a latent bug in the shipped I/O path, most reachable on an already-ready fd (loopback), not something the join work introduced.
- **A suspended task releases its worker; that is the whole point, and it is testable.** [examples/pass/async_yield_smoke.yoop](examples/pass/async_yield_smoke.yoop) runs under `YOOP_NUM_WORKERS=1` and parks two tasks on two pipes simultaneously. If suspension stops actually freeing the thread, the single worker parks inside the first read and the test **hangs rather than fails**. The bridge is `conc.awaitReadable(fd)`: `yoop_io_arm_readable` registers a one-shot interest against the current task (thread-local, set by the worker around each step) and returns, then `await suspendNow()` parks the coroutine; the multiplexer calls `yoop_task_make_runnable` on readiness. `arm` returning **1** means "no current task" (called off a worker), and the caller must fall back to the blocking wait rather than suspend into a hole nothing can resume it from.
- **`Readable.read` / `Writable.write` are ASYNC, and that colors everything above them.** The chain is `ffiRecvAsync` -> `TcpStream.read` -> `Readable.read` -> `std/http`'s `readSome` -> `readHead`/`readBody` -> `exchange` -> `serveConnection` -> `serve`, so all of those are `async` and every entry point bottoms out in the sync/async bridge (`main` spawns a `task`, then `wait`s it). Two deliberate stopping points keep the blast radius down: **`Handler.handle` stays synchronous** (a handler takes a buffered request and fills a response - nothing to await; the hello_server e2e asserts its `define` has no `presplitcoroutine`), and **`Writable.flush` stays synchronous**. An in-memory implementer just declares `async` and never suspends - an async function that does no awaiting runs straight through on its first step. **Sockets must be `O_NONBLOCK`** for any of this to work (`newStream` and `ffi_listen` set it), because the async path is "try the syscall, and on EAGAIN arm + suspend". A `TcpStream` carrying an ambient `timeoutMs`/token still routes to the BLOCKING cancel-aware helpers - that path parks a thread.
- **`alwaysDiverges` ([diverge.js](src/jsyooptypecheck/diverge.js)) is the single authority on "does control leave here", and it answers two questions.** It gates the `? e { ... }` handler block AND the missing-return check (`checkAllPathsReturn`, run from both `validateFunction` and `validateMethod` in [checkStatement.js](src/jsyooptypecheck/checkStatement.js)) - one analysis, so the two can't disagree. Non-obvious cases it MUST keep handling, each found by a real false positive: **`while (true)` with no escaping `break`** diverges (otherwise every accept/event loop reads as a missing return), and `hasEscapingBreak` stops descending at nested loops AND at `switch` (a switch arm's `break` targets the switch's end label, not the loop); **a block-owning kind's `trailingBlock`** counts, because `ephemeral scope(a) { ... return v; }` parses as a CONST_DECL carrying the block rather than a BLOCK statement, so the return is invisible to a naive walk (this is what [examples/playground/diskscope](examples/playground/diskscope) `relayout` does). Everything unlisted answers false: over-reporting costs a user one explicit `return`, under-reporting lets a handler block fall through into an uninitialized binding. Adding a statement kind that can carry a body means adding it here. **It answers a third question now: dead code.** `firstUnreachableIndex(body)` in the same file is its dual (everything after the first diverging statement in a block is unreachable), called from `checkBlock` - the one funnel every block in the language passes through, so function/method bodies, if/loop/switch-arm bodies and a kind binding's `trailingBlock` are all covered by that single call site. The conservatism runs the right way for a diagnostic: `alwaysDiverges` says `true` only when divergence is certain, so this never flags live code and only misses some dead code.
- **`expr? e { ... }` puts STATEMENTS inside an EXPRESSION, which is a new shape in this compiler.** The handler form (Phase 10.E.3) runs a block on the Err path instead of propagating, so three things had to give. (1) **checkExpr.js now imports `validateStatement` from checkStatement.js** - a cycle, but the same shape as the existing checkExpr <-> typecheck.js one and safe for the same reason (neither binding is touched at module-eval time). (2) **Codegen carries an ambient `currentStmtCtx`**, saved/restored in `emitStmt`, because `emitExpr` takes no ctx and the handler block needs the enclosing break/continue labels to lower `break` / `continue`. Same ambient-state pattern as `currentCoro` / `currentReturnType`. (3) **The block must diverge on every path**, decided by `alwaysDiverges` in [diverge.js](src/jsyooptypecheck/diverge.js) - the block runs *instead of* producing a value, so a fallthrough would leave the binding it feeds uninitialized. `SWITCH_STATEMENT.isExhaustive` is stamped in `checkSwitch` for this. The handler form deliberately does **not** require the enclosing function to return a fallible variant - that is the point of it, and it is what makes the form usable in `main` or a handler returning a plain value. It also skips `emitPendingCleanups`: whichever terminator the block uses fires the cleanups for ITS exit. The legacy single-module codegen path throws on it, like it does for template literals and ranges.
- **`await f(x)?` needs a parser swap, and `?` inside a coroutine is not a `ret`.** The postfix `?` binds inside `await`'s operand parse, so `await f(x)?` naturally parses as `await (f(x)?)` - backwards. `parseExpression`'s await branch detects a `TRY_OP` operand and swaps the two nodes (the ambiguity Rust spells around with `foo().await?`). Separately, the `?` early-return path in codegen has to store through `%__ret` and branch to the coroutine's final-suspend trailer; emitting a bare `ret` produced IR whose return value did not match the coroutine's `ptr` result type.
- **Async survives every dispatch form, each for a different reason.** Concrete trait calls mangle as before with the coroutine ABI. **Generics** work because `cloneAstWithSubstitution` resolves `boundMethod` into a concrete `calleeMangledName` during monomorphization - so by codegen there is no abstract receiver and generic == concrete; the two fixes needed were carrying `isAsync` through `substituteTypeParams` and calling `noteAsyncCallee` from `resolveGenericCall` (which bypasses normal call resolution). **Vtables** work because `validateVTableDecl` stamps the trait method's asyncness onto the slot's `FunctionPointerType` - users never write `async` on a `=>` annotation, and the trait is already the authority for the slot's params and return type.
- **The multiplexer's abandon handshake is `fired`-under-`io_mu`, and breaking it is a use-after-free.** A wait's state (`yoop_io_wait_t`) lives on the PARKING THREAD'S STACK, so once a wait can be abandoned (deadline or cancel) the I/O thread has to be provably done with it before that frame dies. The protocol: the multiplexer sets `fired = 1` and calls `yoop_unpark` **while holding `io_mu`**; an abandoning parker takes `io_mu` and checks `fired` - set means the multiplexer already finished (report ready), clear means the parker deregisters from kqueue/epoll and drops the table entry, after which the multiplexer can never reach the frame again. Registrations are keyed by a never-reused **sequence number**, not by the wait struct's address, because a later wait can land on the same stack address and a stale event would otherwise be misdelivered to it. Lock order is `io_mu` -> park-token mutex, never the reverse. Same rule for cancel tokens: `yoop_cancel_request` unparks under the token's mutex, so `yoop_cancel_remove_waiter` returning proves no unpark is still in flight.
- **One waiter per `(fd, direction)` in the multiplexer.** epoll's `EPOLL_CTL_MOD` and kqueue's `EV_SET` both *overwrite* the stored user payload, so a second concurrent registration silently stole the first waiter's wakeup and stranded it forever. A registration table now rejects the second with `EAGAIN` (which is what the 8.F.2 plan always specified). If you ever want genuine multi-waiter fds, that needs a per-fd waiter list plus a story for the thundering herd against BLOCKING sockets - not a one-line change.
- **`yoop_now_ns` is the monotonic deadline clock; `yoop_wall_ns` is the timestamp.** Every timed wait in the runtime goes through `yoop_cv_wait_until_locked`, which is pinned to the same clock, so `yoop_now_ns() + duration` is always a valid deadline. Using `yoop_wall_ns` as a deadline base reintroduces the bug where an NTP step moved every in-flight deadline.
- **A token deadline elapsing is `TimedOut`; only an explicit `request` is `Cancelled`.** `yoop_cancel_requested` folds both together (the "should I stop?" poll) while `yoop_cancel_flagged` isolates the explicit cancel - blocking calls that are *doing work* branch on the latter so the two reasons stay distinguishable at the call site. The exception is `yoop_cancel_wait`, which *observes* a token rather than doing work: there a token firing for either reason is `Cancelled` and `TimedOut` means the caller's own deadline won.
- **`TcpStream` carries ambient `timeoutMs` + `token` and is constructed in exactly one place.** `Readable.read` / `Writable.write` have fixed trait signatures, so per-stream deadlines ride on the struct (the `SO_RCVTIMEO` shape) rather than in the signatures - which is what lets all of `std/http` inherit timeouts with no change. Both default to off and the no-timeout path still calls the original `ffi_recv` / `ffi_send_all`. Every construction goes through `newStream(fd)` in [std/net/tcp.yoop](std/net/tcp.yoop), so adding a field does not mean touching every accept/connect return. The trait methods have to squeeze a three-way outcome into `Result<c_ssize_t, string>` (a timeout arrives as `Err "timed out"`); callers that must distinguish use `tcpReadCt` / `tcpWriteAllCt` / `tcpAcceptCt` and get the `IoOutcome` / `AcceptOutcome` variants.
- **Windows is a supported target, and five of its rules are load-bearing.** The port is real (full `npm test` passes there), but it constrains what new code may call.
  - **The multiplexer is split per platform, Go-netpoll style, and Windows is a real IOCP backend.** [yoop_io.c](runtime/yoop_io.c) is a platform-neutral core (registration table, seq identity, the fired-under-io_mu abandon handshake, public entry points); the engines are [yoop_io_kqueue.c](runtime/yoop_io_kqueue.c), [yoop_io_epoll.c](runtime/yoop_io_epoll.c), and [yoop_io_windows.c](runtime/yoop_io_windows.c), behind the contract in [yoop_io_internal.h](runtime/yoop_io_internal.h). Read that header before touching any of them - it carries the whole argument for why they are split.
    - **Readiness is not portable, so the shared contract is the OPERATION.** kqueue/epoll say "this fd is readable now, go call recv"; IOCP says "the recv you started has finished". The forcing discovery: **write-readiness cannot be expressed on IOCP at all** - a zero-byte `WSASend` completes immediately whether or not the send buffer has room, so there is nothing to wait on. Hence `yoop_iop_recv_begin` / `send_begin` / `accept_begin` + `yoop_iop_end`: POSIX implements them as "nonblocking syscall, on EAGAIN arm and retry on resume", Windows as overlapped `WSARecv`/`WSASend`/`AcceptEx`. Neither pretends to be the other.
    - **Accept is an operation, and that one is a trap.** It is tempting to leave it on readiness since a readable listener means "connection pending" - but IOCP expresses read-readiness with a zero-byte `WSARecv`, and `WSARecv` on a LISTENING socket fails with `WSAENOTCONN`. There is no probe for a listener on a completion port; `AcceptEx` is the only way. (Found the hard way: the HTTP loopback fixture failed with "not connected".)
    - **`yoop_iop_*_begin` must call `io_ensure_started()` before dispatching to the backend.** `io_mu` is created by `io_start_locked`, so a completion backend that locks it first locks an uninitialized mutex - which segfaults rather than failing cleanly. This bit once already.
    - **Every socket close must route through `yoop_io_closing`, and this is correctness, not hygiene.** IOCP caches which sockets are bound to the completion port (associating twice fails, and there is no "is it bound?" query), and **Windows recycles socket handle VALUES**. A stale cache entry therefore makes the next socket with that same integer look already-bound, `CreateIoCompletionPort` is skipped, and operations on it never deliver a completion. The failure is a HANG that appears only after enough connections to recycle a handle - it reads like a race and is not one. `yoop_sock_close` and `yoop_socketpair_close` both call it; a new close path must too.
    - **A registration for an OPERATION outlives delivery; one for plain readiness does not.** `yoop_iob_deliver` drops the entry only when `kind == YOOP_OP_NONE`, because an operation's byte count has to survive until the resumed task (`yoop_iop_end`) or the woken thread (`yoop_iop_wait`) reads it. Getting this wrong on either branch hangs that flavor.
    - **The buffer handed to a `begin()` must outlive the suspend.** On Windows the kernel writes through it while the task is parked. This is the one rule the operation API adds over the readiness API; every std/net caller passes connection-owned storage, not a local temp.
    - **`pipe()` is not waitable on Windows**, so anything handed to `yoop_io_wait_*` / `yoop_io_arm_*` must come from `yoop_socketpair` (pipe on POSIX, loopback TCP pair on Windows) and be read/written through its `yoop_socketpair_read/write/close` siblings - a Windows SOCKET is not a CRT fd. `concurrent_pipe.yoop` and `async_yield_smoke.yoop` depend on this; a pipe there would *hang* rather than fail.
    - The filesystem/dirent helpers that used to share yoop_io.c now live in [yoop_fs.c](runtime/yoop_fs.c). Adding any runtime `.c` still means updating **both** `RUNTIME_SOURCES` in [runtimeBuild.js](src/runtimeBuild.js) and the mirror list in [run_tests.sh](runtime/tests/run_tests.sh).
  - **std/net must not call libc sockets directly.** A Windows socket is a `SOCKET` handle (not an fd, so `close` is wrong and `closesocket` required) and reports failures via `WSAGetLastError` (so `errno` is left stale and every `errno.message(errno.get())` printed nonsense). [socket_ffi.yoop](std/net/socket_ffi.yoop) therefore externs the `yoop_sock_*` shims in [yoop_net.c](runtime/yoop_net.c), which present the POSIX shape - int descriptors, -1, errno set - on every platform and are plain passthroughs on POSIX. New socket surface goes through a shim, not a fresh libc extern.
  - **Header order in the runtime is fixed by [yoop_platform.h](runtime/yoop_platform.h) and must stay there.** `<windows.h>` pulls in the legacy `<winsock.h>` unless `WIN32_LEAN_AND_MEAN` is set, and it conflicts with `<winsock2.h>`; the header every TU already includes establishes `winsock2 -> ws2tcpip -> windows` once so no new file has to rediscover it. The same header owns `yoop_thread_spawn/join` and the `yoop_wsa_to_errno` / `yoop_sock_fail` bridge.
  - **stdout/stderr are put into BINARY mode by `yoop_runtime_init`.** The MSVC CRT otherwise rewrites every `\n` into `\r\n` on the way out, which both corrupts binary payloads and made compiled programs' output differ from macOS/Linux for no visible reason. Same call site starts Winsock (`yoop_net_startup`), since std/net reaches sockets by paths that never otherwise touch the runtime.
  - **Debug info on Windows is CodeView, not DWARF** - clang drives the MSVC target with `-gcodeview`. `-g` works and the build is fine; the DWARF-via-lldb e2e tests self-skip there (`dwarfSkipReason` in [e2e.test.js](src/e2e.test.js)). Getting a debugger working on Windows is unfinished work, not a solved problem.
  - **`opengl32.dll` exports OpenGL 1.1 and nothing newer, so `framework:OpenGL` needs a loader, not just a library.** Every shader / VAO / VBO / uniform entry point (GL 1.5+) belongs to the display driver rather than to the OS and has to be resolved at run time against the CURRENT CONTEXT via `wglGetProcAddress`. GLEW and glad cannot be linked to solve this: they expose the plain `glCreateShader` spelling as a preprocessor macro over `__glewCreateShader` / `glad_glCreateShader`, and a macro is invisible to yoop codegen, which emits a call to the literal symbol named in the extern block. [runtime/yoop_gl_win32.c](runtime/yoop_gl_win32.c) is therefore a table of real forwarding functions with the real names, each resolving lazily on first call. It is **not** in `RUNTIME_SOURCES` - `glueSourcesForLinkFlags()` in [runtimeBuild.js](src/runtimeBuild.js) adds it only for a program that actually named OpenGL, and `lowerLinkFlag` maps `framework:OpenGL` to `-lopengl32` for the 1.1 half. An entry point missing from the table is an ordinary "unresolved external symbol glFoo" at link time; the fix is one line.
  - **A C function returning a bare pointer must NOT be declared as `T[]`.** A yoop array is a 16-byte `{ data, len }`. Under SysV that comes back in rax:rdx, so the pointer lands correctly and the junk `len` is simply never read - the mismatch is invisible on macOS and Linux. The Win64 ABI returns a 16-byte struct through a HIDDEN FIRST ARGUMENT, so the callee sees our return slot as its first real parameter: it writes through it, and the caller reads what it wrote back as the data pointer. Declare `unsafe_ptr<T>` and pair it with `unsafe_ptr.toArray<T>(p, n)`, which also gets a real length. (Found via `SDL_GetKeyboardState` in the nebula_arena demo, whose key reads were garbage on Windows only.)
  - **`setvbuf(stdout, NULL, _IOLBF, 0)` is not just a no-op on Windows - it kills the process.** The MSVC CRT has no line buffering (it downgrades `_IOLBF` to `_IOFBF`) and rejects a size of 0 through its invalid-parameter handler, which fast-fails with `STATUS_STACK_BUFFER_OVERRUN` (0xC0000409) before `main` gets anywhere. `yoop_stdout_linebuf` uses `_IONBF` there instead, which is the closest thing the CRT offers to the guarantee the function makes.
  - Toolchain resolution lives in [src/toolchain.js](src/toolchain.js) and is shared by the driver and both test harnesses, so a test can never pass on a machine where the real driver fails. It locates clang, finds MSVC's `link.exe` via `vswhere` (clang needs it; `-fuse-ld=link` names it deliberately, as `lld-link` is absent from some LLVM packages and broken in others), adds the Windows-only flags, and maps link-flag names (`m` and `pthread` do not exist as separate libraries there). `librarySearchArgs()` is the same idea for `-L`/`-I`: Homebrew prefixes on macOS, vcpkg and the conventional unzipped-SDK prefixes on Windows, with `YOOP_LIB_PATH` / `YOOP_INCLUDE_PATH` as the explicit override. Anything that shells out to clang must go through it.
- **Codegen requires `node.resolvedType` on every node.** Codegen does zero type-checking. If you add an AST kind, the typechecker must set `.resolvedType` on it before codegen sees it, or codegen will crash or emit wrong IR.
- **Identical string literals are interned into ONE global, and `enum<string>` equality depends on it.** `==` lowers to `icmp eq ptr` (INT_OP_MAP) for every type including strings, so comparing two string-backed enum cases is only correct when both sides name the same global. That used to be left to the linker's constant merger, which dedupes identical `unnamed_addr` constants on Mach-O/ELF but not for MSVC at -O0 - so `dir == SortDir.Asc` silently evaluated false on Windows while passing everywhere else. `emitRawStringGlobal` now pools by encoded content, making the guarantee explicit. Note plain `string == string` is a typecheck error, so enum cases are the only string comparison in the language; if that ever changes it needs a real byte compare, not pointer identity.
- **`llvm.coro.end`'s result must be DISCARDED, not bound to a temp.** Its signature changed in LLVM 19/20 from `i1 (ptr, i1)` to `void (ptr, i1, token)`. Newer LLVM auto-upgrades the old spelling on read, but if the call's result is bound to a name the upgrade rewrites the call to `void` and leaves the `%tN =` in front of it - the module then fails verification with "Broken module found", which reads like a codegen bug and is not one. Discarding is legal under both signatures, so one spelling works across every supported LLVM without version detection.
- **A pass-A type SHELL must be FILLED IN PLACE, never replaced.** This is the single most expensive invariant in the typechecker to get wrong, and it has now been violated and fixed three separate times (variants in 13.A, vtables in 9.G, structs while merging std/db into a directory module). Pass A registers a name with an empty body; pass C resolves the body. Any field, parameter, or payload that resolved to that type in between holds a REFERENCE to the shell object - so building a fresh populated type and swapping the table entry leaves those references pointing at an empty type. `sizeOfType` then reports no fields, every enclosing struct is undersized, and the emitted IR reads its own fields at the wrong offsets. Symptoms, in ascending order of nastiness: the misleading `type "T" has no field "f"` on a field that IS declared; a compiler CRASH (`detectRecursiveField` walking a shell's null `fields`); and, across the source files of a directory module where nothing forces a declaration order, a SILENT MISCOMPILE. Structs use `StructShell` (deliberately unfrozen) + `fillStructShell` in [types.js](src/jsyooptypecheck/types.js). If you add a nominal type kind, register an unfrozen shell and fill it - do not construct-and-swap. Regression fixture: [examples/pass/decl_order_independence.yoop](examples/pass/decl_order_independence.yoop).
- **Instantiating a generic SNAPSHOTS the generic decl, so generic bodies resolve before any concrete decl.** `instantiateStruct` copies `genericDecl.genericFields` into a fresh cached type; a snapshot taken before the generic's own body resolved is permanently field-less (`type "Bag__int32" has no field "item"`). Pass C stage 1 is therefore split into two sub-stages - every generic TYPE body in the module group, then everything else. Same reason the Phase 7.2 generic-TRAIT pre-pass exists. In-place shell filling does NOT cover this case, because substitution builds new field types rather than sharing the generic's.
- **Types are immutable (`Object.freeze`'d) - except `KindType`, `TypeParamType`, `VTableType`, and a `StructShell` before it is filled.** `KindType` mutates during pass C.2 to fill in resolved clause details. `TypeParamType` mutates *once* during pass C of Phase 7.2 / 9.J to fill in its `bounds: TraitType[]` slot (one entry per `implements` clause; `<T implements Display>` → length 1, `<T implements (A, B)>` → length N); after that it's effectively immutable. `VTableType` mutates once in pass C.3b: pass A registers a shell (empty `fields`/`methodOrder`, null `traitModuleId`) and `validateVTableDecl` fills that same object. It used to build a fresh populated type and swap the table entry, which left any struct field that had already resolved `d: MyVtable` pointing at the shell - and a shell has no method slots, so a same-module `MyVtable.method(ref x.d, ...)` failed with "vtable has no slot for trait method" while the identical code across a module boundary worked. `TraitType` is itself frozen, but its `methods: Map` and (Phase 9.J) `extendsTraits: TraitType[]` slots are mutable containers that pass C.1 populates — the outer object is sealed at construction, the maps/lists inside are not. Don't freeze KindType/TypeParamType/VTableType; do treat every other type object as immutable.
- **`sizeOfType` must match the layout codegen actually emits, and under-reporting is a memory stomp.** It sizes variant payload buffers (`{ i32, [N x i8] }`) and `Vec`/array allocations, so a type it reports too small gets a buffer that later stores run past. Two cases were wrong and are fixed: a variant returned `4 + maxPayload` (missing the one-byte floor a payload-free variant occupies and the trailing pad, which corrupted any variant nested in another variant's payload), and a `vtable` had no case at all and fell through to the default 8 (so a `Vec` of structs embedding a `Dispatcher` allocated half of what it wrote). Over-reporting is harmless; when adding a type kind, add its `sizeOfType` case.
- **Struct literals can't be typed standalone.** A bare `Foo { x: 1 }` returns an error from `resolveExprType`. Struct literals must be pinned to a target type via `checkInitializer` (assignment RHS, return value, call argument, etc.).
- **Binding type inference (the annotation is optional).** A `let`/`const` may omit its `: type` when it has an initializer (SPEC §4: "required without an initializer; optional when the initializer is unambiguous"). The parser leaves `typeAnnotation = null`; the type is inferred from the initializer in the typechecker. For locals this is in `checkLetOrConst` ([checkStatement.js](src/jsyooptypecheck/checkStatement.js)): `resolveExprType` the RHS, then `concretizeInferred` defaults `untypedInt` -> `int32` / `untypedFloat` -> `float64` (recursing into array elements, and re-pinning untyped array literals via `checkArrayLiteralWithElemType`) so the binding gets a concrete type identical to what an annotation would produce. For **module-level** bindings the inference is deferred to pass D.0 (`validateModuleInit`), because pass C.4 hasn't built the full per-module typeContext yet; the inferred type is published into `typeContext.moduleSymbols` (= `env.localSymbols`) so importers - which typecheck later in topological order - resolve the name correctly. Pass C.4 leaves `resolvedType = null` as the "infer later" sentinel (distinct from an `ErrorType` failure). Anything that can't be typed standalone (bare/orphan struct literals, empty array literals) yields a `cannot infer a type for "x"; add an explicit type annotation` error. Codegen is unchanged - it only ever sees a concrete `node.resolvedType`.
- **A `for (let i = ...)` counter is the ONE place the literal default is overridden by context.** `declareForCounter` ([checkStatement.js](src/jsyooptypecheck/checkStatement.js)) types an unannotated counter from the *loop condition*: if the condition is a comparison (`COUNTER_PINNING_OPS`) with the counter on one side, the other side's concrete numeric type wins, so `for (let i = 0; i < xs.len; i += 1)` gives `usize` where a plain `let i = 0;` gives `int32`. Without that shape it falls back to `concretizeInferred`. The rule exists because `int32 < usize` is a hard error (`unifyArith` does no implicit widening), which would have made the most common counted loop fail on the very condition the counter serves. The initializer is probed on a **throwaway error channel** and then re-checked through `checkInitializer` on the real one, so a broken initializer reports once and an untyped literal still gets pinned (codegen must never see an `untypedInt` in the init store). The counter lives in a scope covering the head and body only, and both codegen emitters (`emitForLoop` single-module, `emitForLoopStmt` multi-module - the same near-duplicate-pair hazard as printf) must open that scope and emit the alloca.
- **The concurrency kinds are declared in std, not built into the compiler.** `task`, `async`, `joined`, `pooled` and `Task` are ordinary `kind { ... }` decls in [std/core/kinds.yoop](std/core/kinds.yoop) (autoloaded into every module graph), and they lex as ordinary identifiers - the same precedent `test`/`suite` set. Their behavior comes from clauses: `pausable` makes a function a coroutine, `provides Task` rewrites the call-site result type, `refcounted <retain> <release>` names the methods the compiler calls, and **storage is derived** (`mustNotEscape scope` -> stack alloca, `refcounted` -> heap). `REQUIRED_CORE_KINDS` in [typecheck.js](src/jsyooptypecheck/typecheck.js) is the carve-out: it asserts std declares each one with the clauses the compiler consults, erroring by name if not. The irreducible built-in is `Task<T>` SATISFYING `Shared`/`Joinable` (it is a compiler type, so it cannot carry an `implements` list) - see `taskSatisfiesKind` in [coreKinds.js](src/jsyooptypecheck/coreKinds.js). Design + as-built: [plans/kinds-in-std.md](plans/kinds-in-std.md).
- **A function decl is an optional run of kind prefixes, then an optional `function`** (SPEC section 7). `task fetch(...)` and `task function fetch(...)` are equivalent, and prefixes stack (`async disposable open(...)`, subject to each kind's `appliesTo`). `kindPrefixedFunctionArity()` in the parser is the structural recognizer; which prefixes are legal is a typecheck question.
- **Two unrelated meanings of "kind" on a binding.** `binding.kind` = mutability (`"let"` / `"const"` / `"discard"`). `binding.kindType` = the Phase 6 user-defined kind declaration. Same word, orthogonal semantics.
- **Imported structs may be shells mid-pass.** Pass A registers struct types with `fields: null`; pass C fills them in. Don't assume a `StructType` from an imported module is fully populated before pass C completes.
- **A struct used as a variant payload must be declared BEFORE the variant, in the same file.** Same shell hazard as the bullet above, but within one module: `variant V { Case { f: T } }` where `type T` appears later resolves `T` against its unpopulated shell, and the constructor literal then fails with the actively misleading `type "T" has no field "<field>"` pointed at the use site (the field IS declared; the decl is just too late). Declaration order is not otherwise significant in Yoop, so this is a papercut, not a rule worth keeping - writeup and fix candidates in [plans/sqlite-binding-papercuts.md](plans/sqlite-binding-papercuts.md). Note a variant case name sharing a name with a same-module type is NOT a problem (verified) - only the ordering is.
- **Fallible `err` fields unobserved at scope exit are compile errors, not warnings.** Enforced in `popScope` via [fallible.js](src/jsyooptypecheck/fallible.js). Adding a new way to produce a fallible value means making sure `err` observation is tracked.
- **Error control flow differs by stage.** Lexer + parser throw and abort on first error. Typechecker accumulates into an `errors` array. Codegen assumes a clean AST and will crash on malformed input.
- **Warnings ride the errors array and are split out at the end; `errors` always means HARD errors.** `pushWarning` ([errors.js](src/jsyooptypecheck/errors.js)) appends to the SAME `ctx.errors` every `pushError` uses, tagged `severity: "warning"` plus a stable kebab-case `code`. Sharing the array is what keeps the plumbing free: one channel threaded through every check function, and `stampErrorOrigin` covers both severities without knowing they differ. `typecheckProgram` filters at its `return` into `{ errors, warnings }`, so every existing `errors.length === 0` gate keeps exactly the meaning it had (a warning must never fail a build). **The legacy single-module `typecheck()` path DROPS warnings** rather than returning them, because its callers report whatever is in `errors` as a hard failure. Consumers: the driver prints warnings after `typecheck: ok` and leaves the exit code alone, filtered to non-std modules (std is autoloaded into every graph, so a warning there would attach to every compile in the world and be unfixable by whoever is reading it; `--warn-std` opts back in). The LSP publishes them unfiltered, since the editor squiggles whatever file you have open.
- **A diagnostic `code` selects the LSP rendering, and that is why dead code is dimmed rather than squiggled.** `TAGS_BY_CODE` in [server.js](src/lsp/server.js) maps the typechecker's code onto an LSP `DiagnosticTag`; `unreachable-code` gets `Unnecessary` (1), which editors render faded out. Severity alone cannot express this - a plain warning underlines code whose only problem is that it does not run. A new warning that wants non-squiggle rendering adds a line to that map, not a new field.
- **`sourceLoc` cannot anchor a span; `startLoc` and `endPos` can.** `buildSourcedNode` stamps the parser's `pos`, which is the offset PAST the lookahead token, so a statement node's `sourceLoc` points into its own middle (`return 1;` lands on the `;`, `let x: T` on the `:`). Fine for naming a line, useless as the start of a highlight. `parseBlock` therefore stamps `stmt.startLoc` (the statement's real first token) on every statement it collects and `node.endPos` (the offset of the closing `}`) on the block, which is what lets the unreachable-code warning span an exact region. Only blocks get this. Anything else needing a span needs the same treatment rather than trusting `sourceLoc.pos`. Note `formatDiagnostic` ([helpers.js](src/helpers.js)) clips its caret run to the end of the line it starts on, since a multi-line span would otherwise draw carets off into empty space.
- **Source locations on every AST node are load-bearing.** Diagnostics rely on `node.sourceLoc` being set. Use `buildSourcedNode` (parser) or copy `sourceLoc` when synthesizing nodes elsewhere.
- **Generic decls live in a separate table from concrete ones.** The `structTable`/`funcTable`/`traitTable` only ever contain monomorphic types - generic decls go in `genericStructTable`/`genericFuncTable`/their trait equivalent and are instantiated lazily into the registry by `resolveTypeAnnotation` (for explicit type apps) and by call-site inference (for generic functions). Never put a `TypeParamType`-bearing type into a concrete table, and never reach across into the generic table when you mean to look up a concrete type.
- **Codegen never sees `TypeParamType`.** Generic struct/function bodies that reach codegen have been pre-substituted via `cloneAstWithSubstitution` so every `resolvedType` is concrete. If a `TypeParamType` ever appears in codegen, it's a typechecker bug - instantiation was missed at the use site.
- **Type aliases are transparent and live in their own table.** `type NodeId = usize;` parses to a `TYPE_DECL` with `targetType` (a type annotation) and NO `fields`; pass A registers it in the per-module `aliasTable` (name -> `{ annot, sourceLoc }`), never in `structTable`. Resolution is lazy and transparent: the `typeName` branch of both resolvers (`resolveTypeAnnotationInModule` in [typecheck.js](src/jsyooptypecheck/typecheck.js) and `resolveAnnotMulti` in [instantiate.js](src/jsyooptypecheck/instantiate.js)) consults the shared `lookupAlias` ([types.js](src/jsyooptypecheck/types.js)) and, on a hit, resolves the alias RHS *in the alias's home module* with a cycle-guard set - so the result IS the underlying type (no distinct identity, no special-casing for indexing/coercion, and **codegen never sees the alias name**). Cross-module aliases ride `importedNames` with `kind: "alias"`; namespaced (`ns.NodeId`) and chained aliases work. Pass C validates each alias RHS once (unknown target / cycle is a decl-site error) and stamps `d.resolvedAliasType` (NOT `d.resolvedType`, which would make codegen try to emit a struct). Generic aliases (`type Pair<T> = ...`) and composed RHS (`A & B`, `A | B`) are deferred - the deferred-feature diagnostic fires in pass A. The legacy single-module typecheck path does not support aliases (skips them).
- **Trait method calls are always trait-qualified (Phase 7.4).** Source form is `Trait.method(ref x, ...)`. Bare-form `method(ref x, ...)` is a typecheck error with a fix-it hint. The dotted form `x.method(...)` is also rejected (unchanged from Phase 5). Mangled symbols are `<structModuleId>__<StructName>__<TraitName>__<methodName>` via [mangleTraitMethod.js](src/jsyooptypecheck/mangleTraitMethod.js) - one LLVM `define` per (trait, method) pair, so cross-trait same-name impls work because each call site resolves to a distinct symbol. The typechecker tags every trait call with `calleeMethodOf` + `calleeTrait` + `calleeMethodName` + `calleeMangledName` (struct receiver) or `boundMethod` (TypeParamType receiver, rewritten post-substitution by codegen). **Phase 9.J: extends chain.** A trait declared `trait Child extends Parent { ... }` registers `Parent` in its `extendsTraits` slot during pass C.1. `validateImplBlock` flattens the chain into `StructType.implementsTraits` (every ancestor reachable from a user-declared trait is added, deduped by identity), so trait-method lookup against a struct never re-walks `extends`. The qualifying trait at a call site can be a descendant of the trait that *declares* the method (`BatchIterable.next(...)` where `next` is declared on `Iterable`); `resolveTraitQualifiedCall` walks the qualifying trait's extends chain to find the declaring trait and mangles using its name, so dispatch lines up with the single `Struct__<DeclaringTrait>__<method>` define. Multi-bound type params (`<T implements (A, B)>`) work the same way: bound-satisfaction and bound-method dispatch both `walkTraitExtends` across every bound.
- **`propagates<K>` is ADVISORY, not enforced (ownership redesign 2026-06-17).** See [plans/ownership-and-typestate-redesign.md](plans/ownership-and-typestate-redesign.md). The earlier "Phase 6.4 strict" model (a hard must-handle-or-hand-off contract) was reversed: the compiler is now **silent by default** about resource lifetimes. A type that declares `propagates<K>` is documenting "bindings of me own a resource" for readers and tooling; it does **not** force the user to do anything, and returning a propagating value never requires the function to declare `propagates<K>`. There are **no unsatisfied-obligation errors on the ownership side**. Avoiding double-free is the `dispose` implementer's responsibility (make `dispose` idempotent: free-then-null, guard on null). The two things that still have teeth:
  1. **Auto-cleanup (the one opt-in with codegen)**: declare the kind keyword on the binding (`disposable arr: T = ...`) and the compiler injects the cleanup call at scope end (unless a matching manual `Trait.method(ref arr)` already fired on every path). Same for the refcounted/builtin keywords (`pooled`/`joined`/`Task`) with their own auto-action (release / wait).
  2. **Manual discharge**: a plain `let`/`const` plus an explicit `Disposable.dispose(ref arr)`. Optional, never required.
  Implementation in [kindCheck.js](src/jsyooptypecheck/kindCheck.js): obligations are still computed per binding (`kindType`, `autoCleanup`, `satisfied`; `transferred`/`reported` are now vestigial), but `projectCleanups` only emits cleanup nodes for `autoCleanup` bindings and is otherwise **silent** - the old `reportUnsatisfied` error and the return-site `propagates<K>` transfer/enforcement were removed. `walkExpr` still flips `satisfied` for a matching `Trait.method(ref binding)` call so an already-hand-disposed `disposable` binding doesn't get a redundant injected cleanup. **Path coverage** (so a manual dispose on every reaching arm suppresses the injected cleanup): `IF_STATEMENT` and `SWITCH_STATEMENT` snapshot the sat state, walk each arm from that snapshot, and restore the intersection of reaching arms (diverged-via-`return` arms excluded via `walkDiverged`; SWITCH without a `default` also counts the fall-through path). `WHILE_STATEMENT`/`FOR_LOOP`/`FOR_IN_LOOP` discard inner sat changes (body may run zero times). **`SWITCH_STATEMENT` is now walked** specifically so a `disposable`-keyword binding inside a `case` arm still gets its auto-cleanup injected at the arm-block end (previously SWITCH was unwalked and those cleanups silently never fired). `BREAK`/`CONTINUE` remain no-ops in the walk. `runKindCheck` still runs on generic function decls (`effectiveImplementsTraits` reaches through `genericInstance.declId` into the registry for up-to-date traits). The marker/typestate kinds (next bullet) and `mustNotEscape`/`scoped` escape analysis are a SEPARATE, still-enforced concern - the relaxation only touched the ownership obligations.
- **Region kinds (`appliesTo region`) are anonymous block owners; the use-site shape, not a flag, picks named-vs-anonymous.** A kind decl that says `appliesTo region` (instead of `appliesTo binding`) governs a lexical scope with no named value - the `ephemeral` shape in [std/core/kinds.yoop](std/core/kinds.yoop), for allocator-scope / pushed-context / transaction guards. It MUST declare `ownsBlock` and may NOT also name a value site (`binding`/`parameter`/`field`/`type`/`return`); both are enforced in `populateKindFromClauses` ([typecheck.js](src/jsyooptypecheck/typecheck.js)). The parser recognizes the anonymous form **structurally** - two adjacent idents at statement start where the second isn't a binding name (`looksLikeAnonymousRegionStart` in [parser.js](src/jsyooparser/parser.js)) - and `parseAnonymousRegionBinding` builds an ordinary `CONST_DECL` with `anonymousRegion: true` and a synthesized `$region$N` name (the `$` is unspellable in user source, so it's collision-free and unreferenceable). Everything downstream (kindCheck obligations keyed on `stmt.name`, codegen alloca + trailing-block/implicit cleanup) is **unchanged** - a region binding is just a name-less `disposable` with the cleanup reaching the value by its synthetic name. `validateKindBinding` ([checkStatement.js](src/jsyooptypecheck/checkStatement.js)) is the named/anonymous gate: `appliesTo region` + a name is an error, `appliesTo binding` + no name is an error, and the synthetic name is **not** `declareInScope`'d (the value is unobservable). Both the explicit-block form (`ephemeral EXPR { ... }`, dispose at `}`) and the implicit form (`ephemeral EXPR;`, dispose at enclosing scope end, LIFO) work, mirroring `disposable`. **Companion fix:** the `: type` annotation is now optional on any kind-prefixed binding (recognizer accepts `IDENT IDENT =`, not just `IDENT IDENT :`), and an inferred struct binding type is run through `canonicalizeStruct` ([checkStatement.js](src/jsyooptypecheck/checkStatement.js)) so a call-return *shell* (empty `implementsTraits`) is replaced by the populated structTable entry - otherwise the kind's `requires Disposable` check (and kindCheck's obligation pass, which reads `node.resolvedType` directly and does NOT re-fetch canonical) would spuriously fail on `disposable x = makeThing()`.
- **Marker kinds are a static type-level check, and the kind decl is the authority for transitions.** A kind decl that names a polarity clause (`conferred;` or `restrictive;`) is a *marker* kind: no obligation, no `mustCall`, no codegen. A marker kind names itself in type position (`cleared string`, `tainted uint8[]`) - `parseTypeAnnotation` attaches `kindPrefixes: string[]` to the annotation. There are two enforcement points in [kindFlow.js](src/jsyooptypecheck/kindFlow.js):
  1. **Use-site bound check** at every slot (binding initializer, assignment, return, call argument): `slot.conferred subset-of value.conferred` (a conferred capability cannot be forged; sinks that name it REQUIRE it) and `value.restrictive subset-of slot.restrictive` (a restrictive hazard cannot be silently dropped; plain slots FORBID it). The return site is asymmetric - only the restrictive direction is enforced; the function's signature is the conferring authority (see point 2).
  2. **Decl-authority check** at every function decl: the kind decl pairs a required trait with a method name, and only an impl method of that trait may mediate the transition. A clearance kind declares `requires <Trait>;` plus `clearedBy <method>;` (restrictive) or `appliedBy <method>;` (conferred). For a function whose signature would strip a restrictive kind K (a parameter carries K, the return does not), the function MUST be a METHOD on a type whose `implements` list includes K's required trait, and its name MUST equal K.clearedBy. Same for the conferred direction. **Free functions are categorically rejected** - laundering requires opting in via a trait impl. **Exception, and only for the conferred direction: PASSTHROUGH.** A function is only *conferring* K if it produces a K-carrying value out of one that did not, so when EVERY `return` in the body already yields a value carrying K, no authority is required (`isConferredPassthrough` in [kindFlow.js](src/jsyooptypecheck/kindFlow.js)) - the mirror of the exemption the restrictive direction always had for a parameter that carries K. Without it no wrapper around a conferring API could exist, not even `function f(s: cleared T): cleared T { return s; }`. Two things this depends on: **the conferred half of the authority check runs AFTER the body walk** (`bindingMarkers` only holds params until the walk populates locals, so an up-front check misreads `return someLocal;` as forgery), and **"every" is not "any"** (a body that launders on one path and forges on another is still forging). An empty return list answers false, not vacuously true. This is what makes a `requires`/`appliedBy`-free conferred kind like `owned` usable: its only mint site is a **bodyless `extern` decl**, which `runKindFlow` skips entirely, so the allocating intrinsic is the authority and everything else reaches the kind by passthrough. That in turn is why `funcDeclsByModule` ([typecheck.js](src/jsyooptypecheck/typecheck.js)) collects `EXTERN_FUNCTION_DECL` as well as `FUNCTION_DECL` - without it the marker on an intrinsic's return is silently dropped at every call site. **A marker set is a TREE, not a flat pair of sets.** `emptyMarkers()` carries `args`, mirroring the annotation's `typeArgs`, so `Result<owned string, string>` holds nothing at the top level and `owned` at `args[0]`; `checkBound` recurses positionally. Without that a marker cannot survive a fallible constructor, since the value is only reachable again after a `switch` destructures it. The destructuring side is `payloadFieldMarkers`, which resolves a payload binding's markers from **either** direction: a GENERIC payload (`Ok { value: T }`) maps field -> type param -> type-arg index via `registry.genericDeclById` (the instantiated type has substituted `T` away, so only the generic decl knows which parameter a field came from), while a CONCRETE payload (`Case { f: owned string }`) reads the field's annotation off the VARIANT_DECL AST, reached through the `resolveVariantDecl` lookup built in typecheck.js pass D (a variant TYPE keeps only resolved field types, no annotations). Note a VARIANT_DECL's case list is `variants`; `cases` belongs to ENUM_DECL. **The decl-authority check stays TOP-LEVEL** (it asks what the function hands back, and a `Result` is not itself owned), so the nested position is instead guarded by `checkReturnedConstructor`: at a `return Variant.Case { f: X }` the constructor field is a slot and the return annotation's matching type argument is what it must satisfy. Without it `Ok { value: "a literal" }` would launder a literal into an owned payload. **Companion bug fixed here: `runKindFlow` never walked switch statements at all** - it read `stmt.value` / `stmt.cases`, but a SWITCH_STATEMENT carries `scrutinee` / `arms` / `defaultArm` (arms hold `patterns` + `body`), so every marker check inside an arm body was silently skipped while the identical code one line outside was rejected. Still open: **field-position sources** (a `tainted` struct field surfacing through `x.field`), and constructor field checks at slots other than a `return` (a binding initializer or call argument taking a variant literal). This kills look-alike launderers (`evil(t: tainted X): X` returning a freshly-built `X` that secretly logs the input) and `fake_clear` minters: signature-shape matches don't authorize a transition; impl-of-the-named-trait does. The compiler still bakes in no "launder" verb - the user names the trait and its method; the kind decl just names them as the authority ("expressed sentiment"). The trait method itself has a plain signature (`function cleanse(ref self): T;` on a `Cleansable<T>`); the kind decl describes its semantic role rather than encoding it in the trait method's annotation, because trait methods require `ref self` as their first parameter and Yoop has no `Self` placeholder in return position - so generic traits (`Cleansable<T>` with `T` as the return type) are the idiomatic shape. Use sites: a trait-qualified call `Cleansable.cleanse(ref x)` is recognized by kindFlow via the typechecker-stamped `calleeMethodName` + `calleeTrait` on the CALL_EXPRESSION; the call's result carries every conferred kind whose `(appliedBy, requires.name)` pair matches. Arg sink-checks are skipped on trait calls (the kind decl is the contract).
  Because a binding's type is fixed at its declaration site - either from an explicit annotation or inferred once from its initializer (see "Binding type inference" below) - and assignments must conform, a binding's kind set is likewise fixed at declaration: there is no flow-sensitivity to track, no snapshot/merge, no per-binding kind state. The marker + obligation clauses are mutually exclusive on one kind, and `clearedBy` matches only `restrictive`, `appliedBy` only `conferred` (rejected in `populateKindFromClauses`). `parseKindClause` recognizes `conferred` / `restrictive` / `clearedBy` / `appliedBy` *contextually* inside a kind body so the words stay usable as ordinary identifiers everywhere else. Enforcement covers same-module direct calls (string callee), **imported by-name calls, and namespaced member calls (`ns.fn(...)`)** at parameter/return/binding sites: `kindFlow.runKindFlow` takes a `crossModuleCallee` resolver (built in `typecheck.js` pass D) that maps an imported/namespaced callee to its source decl plus that module's kind table, so a marker on a std-style signature is checked across the boundary instead of silently dropped (`exprMarkers`/`checkCallArgs` resolve via `callNode.calleeModuleId`/`calleeExportName` or `callee.namespaceLookup`; imported kinds share object identity, so the marker sets line up). Still a follow-up: **field-position sources** (a `tainted` struct field surfacing through `x.field` returns empty markers - field kinds are wired only to the ownership `propagates` path, and a `field` appliesTo site trips an ordering bug reporting `(none)`). (A separate fix landed alongside this: trait-qualified calls to an imported *generic* trait - `ImportedGenericTrait.method(...)` - used to fail with "undefined variable" because `lookupTraitByName` checked `imp.kind === "trait"` for the imported-generic branch, but imports.js registers those as `"generic-trait"`; now matched, so the idiomatic generic launder trait `Cleansable<T>` works cross-module.) See [plans/clearance-kinds.md](plans/clearance-kinds.md).
- **Intrinsics enter scope via `extern "intrinsic"` blocks, not auto-injection.** The compiler-recognized intrinsics (`heap_alloc<T>`, `heap_free<T>`, `ctx_alloc<T>`, `ctx_free<T>`, `string_as_bytes`, `string_from_bytes_unchecked`, `bytes_as_string_unchecked`, `array_slice<T>`, plus `wait_until<T>` / `cancel<T>`) used to be installed into every module's `genericFuncTable` at typecheck startup; they're now declared in [std/core/intrinsics.yoop](std/core/intrinsics.yoop) (and concurrency.yoop) inside `extern "intrinsic" from "compiler" { ... }` blocks. A user must `import * as intr from "std/core/intrinsics.yoop"` to call them as `intr.heap_alloc(...)`. The canonical decl objects (with stable `declId` like `$builtin__heap_alloc`) live in `makeBuiltinGenericFuncs()` ([typecheck.js](src/jsyooptypecheck/typecheck.js)) and the per-module `builtinIntrinsicNames: Set<string>` tracks which intrinsics are in scope; checkExpr.js's special-case branches for `wait_until` / `cancel` gate on this set so user code can shadow those names freely if it doesn't import concurrency.yoop. `printf` is the lone exception - it stays globally callable (the name doesn't collide with user identifiers and ~all examples use it). Codegen still dispatches intrinsics by `declId`, not by name; pass C of the typechecker skips type-annotation resolution for canonical generic intrinsics so the `T[]` in their extern-block signatures (documentation only) doesn't need a type-param scope. **`ctx_alloc<T>` / `ctx_free<T>` are the context-routed siblings of `heap_alloc` / `heap_free`** (arena/context allocators, [plans/arena-and-context-allocators.md](plans/arena-and-context-allocators.md)): identical codegen except the `@malloc`/`@free` call becomes `@yoop_ctx_alloc`/`@yoop_ctx_free` (runtime/yoop_alloc.c), which dispatch through the per-thread current allocator (`std/core/alloc.yoop`; default malloc). `Vec<T>` now allocates via `ctx_alloc` and is **container-owned**: it stores the `Allocator` it captured at `vec_new` and `pushAllocator(self.alloc)` around every grow/free, so a Vec built inside an arena scope draws from (and frees into) the arena regardless of the ambient allocator at grow/dispose time. `heap_alloc`/`heap_free` remain the explicit raw-malloc path.
- **Std value imports must be namespaced (`import * as ns from "std/..."`).** [imports.js](src/jsyooptypecheck/imports.js) rejects `import { f } from "std/..."` for any name that resolves to a value (function or const) with a fix-it pointing at the namespace form. Types / traits / kinds / enums / unions are exempt - their declaration-position usage (or capitalization) keeps them from competing with common identifiers. Imports from relative paths (e.g. within std/ itself, or user multi-file fixtures) are unaffected. To call a generic function through a namespace (`vec.vec_new<int32>()`, `intr.heap_alloc(8)`), the resolver consults `srcEnv.genericFuncTable` and routes to `resolveGenericCall` - see `resolveCall` and `checkInitializer` in [checkExpr.js](src/jsyooptypecheck/checkExpr.js), plus the `isNamespaceGenericCall` helper in [checkStatement.js](src/jsyooptypecheck/checkStatement.js) for the let-decl bidirectional-inference path. Codegen reads `node.genericInstantiation` on namespace-prefixed call sites so the emitted mangled name carries the type-arg suffix.
- **Enum and union are nominal types alongside struct, in their own tables (Phase 7.5).** `enumTable` / `unionTable` per module - siblings of `structTable`. Pass A registers shells; pass C fills variant payload field types (enums) and field types (unions). Variant ordinals are **stable 0-indexed integers in declaration order** and codegen emits them as `i32` tag values, so reordering a variant breaks ABI. Codegen reads `node.resolvedEnumType` + `node.resolvedVariant` on `VARIANT_CONSTRUCTOR` / `VARIANT_PATTERN` and `node.scrutineeType` on `SWITCH_STATEMENT` - these are stamped by the typechecker; if any are missing it's a typecheck bug. Untagged-union codegen emits a `{ [N x i8] }` byte buffer; field accesses bitcast through it, which assumes little-endian (the CI matrix is x86_64 + aarch64, both little-endian). Bare `EnumName.Variant` (no payload) parses as `FIELD_ACCESS` and is **promoted in-place** to `VARIANT_CONSTRUCTOR` by `resolveFieldAccess` when the LHS resolves to an enum - codegen never sees a `FIELD_ACCESS` whose object is an enum name.

## Design heuristics

- **Reach for a kind when the obligation moves with the value.** Yoop's kind system (`disposable`, `joined`, `pooled`, `Task`) exists for contracts that have to survive a return, a struct field, or a call across modules - places a comment that says "this MUST be called before X" or a parameter named `safe_*` / `unchecked_*` is doing a kind's job badly. Trigger sentence: "if you have one of these, it means X has happened to it." When you catch yourself writing that, stop and consider a kind. Cost / heuristics / in-tree opportunities (e.g. `validated` on safepath output, `authenticated` on a cleared Request) live in [plans/kinds-design.md](plans/kinds-design.md). One-off programs usually don't earn the cost; std-library APIs and cross-module contracts usually do.

## Phase model

Features land phase-by-phase per the historical roadmap ([plans/archive/roadmap.md](plans/archive/roadmap.md)). Phases 1 through 9 plus library phases A–D are all in; per-phase plan docs live in [plans/completed/](plans/completed/). Recent landings: **cancellation tokens + deadline-aware I/O** (`std/core/cancel/`, a `CancelToken` that can unpark a thread blocked in the multiplexer; bounded `tcpAcceptCt` / `tcpReadCt` / `tcpWriteAllCt` plus ambient per-stream timeouts; a monotonic deadline clock; and the same-fd waiter-conflict fix — see [plans/cancellation-and-io-deadlines.md](plans/cancellation-and-io-deadlines.md), which supersedes 10.F.2.b and 10.F.3); the **std/http rework** (camelCase API, `Result<T, HttpError>` throughout, a non-generic `serve` over the `Dispatcher` vtable, keep-alive, router path captures, new `std/http/wire.yoop` + `std/http/url.yoop`, plus a `transaction` kind and a `DbRef` borrow in `std/db/sqlite/` — see [plans/completed/std-http-rework.md](plans/completed/std-http-rework.md), which also records the four codegen/typecheck bugs it surfaced); Phase 10.E.3 (the handler form of `?` - `expr? e { ... }` handles the failure at the call site instead of propagating it, with the Err payload bound to `e` and the block required to diverge; see the invariant below); Phase 10.E.2 (context strings on `?` - a string or template literal suffix, `expr? "ctx"`, prepended to the propagated error; compiler-side concat for `string` Err payloads plus the `WithContext<T>` trait hook for structured ones — see [plans/completed/phase-10-e-2-qmark-context.md](plans/completed/phase-10-e-2-qmark-context.md)); Phase 9.J (trait `extends` (single, multiple, generic), parenthesized multi-bound type params `<T implements (A, B)>`, and `mustNotShare acrossThreads` enforcement at task-spawn argument sites — see [plans/completed/phase-9-j-reserved-syntax-cleanups.md](plans/completed/phase-9-j-reserved-syntax-cleanups.md)); Phase 9.I (suspendable `wait`); Phase 9.H (`?` over Result-shaped enums); Phase 9.G (`vtable T for Trait` + `=>` function value types); Phase 9.F (`Display` in template literals); Phase 9.D (`for ITEM in EXPR` over arrays); Phase 8.F (task suspension + I/O multiplexer + timers); Library Phases A–D (`std/core`, `std/net`, `std/http` types + parser, `std/http` server); Phase 7.5 (sum types via `enum`, untagged C-style `union`, `switch` with literal + variant patterns + exhaustiveness).

**Async runtime**: `async` functions + `await` lower to LLVM switched-resume coroutines, so a task blocked on I/O releases its worker thread instead of holding it. `std/net` and `std/http` are async top to bottom - [examples/pass/async_server_smoke/](examples/pass/async_server_smoke/) runs an HTTP server plus three concurrent clients on ONE worker thread. Async trait methods work through generics and vtables. See [plans/async-coroutines.md](plans/async-coroutines.md).

**Current focus: the self-hosting bootstrap (10.K)** - rewriting the JS compiler in Yoop, layer by layer, diffing each layer against the JS reference. Read [plans/README.md](plans/README.md) first (the small consolidated plan), then the two north-star docs it points at: [plans/bootstrap-pipeline-contracts.md](plans/bootstrap-pipeline-contracts.md) (the cross-layer data-shape contracts) and [plans/ownership-and-typestate-redesign.md](plans/ownership-and-typestate-redesign.md) (the advisory ownership model the bootstrap follows). The bootstrap source is under [bootstrap/src/](bootstrap/src/). Phase 10's many sub-phases (10.A generic enums, 10.B Iterable `for ... in`, 10.C* `std/collections`, 10.D `std/debug`+`std/log`, 10.E cross-shape `?`, 10.F.1/10.F.2.A cancellation, 10.H alloca uniqueness, 10.I networking polish, 10.X/10.X.2 cleanups) have all landed; the long tail (optimization passes 10.J, in-body cancellation 10.F.2.b, diagnostics, networking follow-ups) is tracked in [plans/archive/phase-10.md](plans/archive/phase-10.md) and lands opportunistically.

Code is annotated with phase comments (e.g. `// 6.5:`, `// phase 6.4:`, `// Phase 7.1:`). Treat these as load-bearing - they mark the version a piece of logic became correct and help future readers locate the spec section.

## Testing yoop code (the `--test` harness)

Distinct from the JS-side tests below: this is how a *yoop program* tests itself. Design + tradeoffs in [plans/testing-via-kinds.md](plans/testing-via-kinds.md). The governing rule is that **testing gets no compiler-baked semantics** - there is no `@test` attribute, no built-in assertion, no reserved keyword. `test` and `suite` are ordinary kinds declared in userland ([std/test.yoop](std/test.yoop)), so `test`/`suite`/`assert` all remain usable as identifiers.

- **A test module** is `*.test.yoop` declaring `import.test;` as its first line and containing no `main`. The flag is what lets `yoopiler foo.test.yoop` work at all (a module with no `main` is otherwise just a compile error) and what makes a mis-named file fail loudly instead of being skipped. It sets `isTestModule` on the PROGRAM node, in the same `import.<feat>;` pragma slot as `import.unsafe;`.
- **A suite** is a top-level function carrying the `suite` kind: `suite function addsNumbers(): void { ... }`. Cases inside it are `let test theory: Case = t.asserts("prose") { ... }` - the binding-position `test` kind fires `recordOutcome` at the block's closing brace on every path out.
- **Assertions are a bool plus a string, deliberately not a comparison.** `theory.isSuccessful` is the outcome; `theory.detail` is a template literal the test writes, reported only on failure. There is no `equals(got, want)` primitive because two operands flatten the context that made a check meaningful. Helpers for repeated shapes are ordinary yoop functions, added as the need shows up.
- **`--test` flow**: glob `**/*.test.yoop` -> syntactically collect every kind-prefixed top-level function -> generate a synthetic entry module in memory (imports + a `main` that hands the suite table to `std/test.yoop`'s `runAll`) -> ordinary pipeline -> link into the existing `mkdtemp` dir -> spawn -> propagate exit code (= failure count). No second codegen path; the generated `main` is *yoop source*, reparsed like `@derive`'s output.
  - The synthetic entry rides `loadModuleGraph`'s existing `readFile(absPath)` overlay (added for the LSP's unsaved buffers). Nothing is written to the user's tree.
  - **Collection is syntactic, then validated.** The driver can't resolve `enumerable as` before typecheck, and typecheck needs the entry module, which needs the names - so it collects every kind-prefixed top-level function and lets typecheck reject any whose kind isn't enumerable into `"suites"`. Safe because a kind-prefixed top-level *function* has no other meaning in the grammar (`task` is a keyword, not an ident prefix).
  - **Diagnostics from the generated module are suppressed** when any real-module error exists. A bad suite breaks the generated suite table too, and a diagnostic pointing into source the user never wrote is worse than none. If generated-module errors are the ONLY errors, they print labeled as a compiler bug.
  - Suite functions get an `EXPORT_DECL` wrapper added by the driver post-graph-load, pre-typecheck, so authors don't write `export` on every suite. Explicit `export suite function` also parses and the wrapper step is idempotent.
- **`appliesTo function` kinds are deliberately narrow.** Only `signature (p: T) => R;` and `enumerable as "<table>";` are legal alongside them, and both are required - a function kind has no value for `mustCall`/`ownsBlock`/`requires` to act on, so those are rejected with a message naming the offending clause. `enumerable as` names the table a consumer asks for, and is the join key that keeps a future `bench` kind (`enumerable as "benches"`) distinct from `suite`. Both clause keywords are *contextual* idents (like `conferred`/`clearedBy`), so `signature` and `enumerable` stay ordinary identifiers everywhere else.
- **Isolation is a kind, not a runner feature.** `runAll` wraps each suite in `ephemeral arenaScope(...)`, so everything a suite allocated is reclaimed in bulk before the next one starts. Suites are the unit of isolation; cases are the unit of reporting. A segfault still takes the process down - TAP lines are written as each case completes rather than buffered, so a crash keeps everything already reported and simply omits the plan line.
- **Filters** ride as extra positionals after the path (`yoopiler --test . addsPlainly`), forwarded to the binary as argv and read via [std/env.yoop](std/env.yoop). Designed in from the start so a future `--isolate=process` (re-exec per suite) needs no compiler change.
- Not yet built (see the plan): `beforeEach`/`afterEach` timings on `mustCall`, and multi-`mustCall` kinds (today `kind isolatedTest = test & ephemeral;` fails with "composition contradiction", which is what blocks composing isolation onto a case).
- Fixtures: [examples/testing/pass/](examples/testing/pass/), [examples/testing/fail/](examples/testing/fail/). These are referenced explicitly by e2e - do NOT put `*.test.yoop` under `examples/pass/`, which is compiled as ordinary programs.

## Test conventions

Tests of the compiler itself, in JS. (For testing yoop code, see above.)

- Node's native test runner (`node --test`), style `describe` / `it` with `node:assert/strict`.
- Unit tests colocated as `<file>.test.js` next to source.
- [src/e2e.test.js](src/e2e.test.js) is the full lex → parse → typecheck → codegen → `clang` → run integration suite. Expensive but authoritative - when in doubt about whether a change works end-to-end, add a small fixture there.
- **[examples/playground/](examples/playground/) is NOT a test surface and does not have to be kept compiling.** Nothing under it is covered by e2e (only [examples/pass/](examples/pass/) and [examples/testing/](examples/testing/) are). A breaking compiler or std change does **not** oblige you to uplift the playground programs - they are scratch space for feeling out ergonomics, and a stale one is expected rather than a regression. Do reach for one when it is genuinely the best available check on a change (todo_api is the CRUD-shaped HTTP consumer, yoopstore the file-serving one, sqlite_demo the FFI one), and say so when you do. If a playground program is found broken, note it and move on; do not let it block or expand the change in front of you.
