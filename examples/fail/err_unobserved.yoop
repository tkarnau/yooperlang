type Bytes {
    len: int32,
    err: string,
}

function read_all(path: string): Bytes {
    return { len: 0, err: "x" };
}

function main(): int32 {
    const r: Bytes = read_all("x");
    printf(`len = ${r.len}\n`);
    return 0;
}
