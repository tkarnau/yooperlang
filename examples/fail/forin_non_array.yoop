// `for ... in EXPR` requires an array RHS, or a type implementing
// Iterable<T>. A plain int32 is neither.
function main(): int32 {
    let x: int32 = 5;
    for v in x {
        _ = v;
    }
    return 0;
}
