trait Disposable {
    function dispose(ref self): void;
}

type T implements Disposable {
    fd: int32,
    function dispose(ref self): int32 {
        return 0;
    }
}

function main(): int32 {
    return 0;
}

// Fails because the impl method `dispose` returns `int32` but the trait signature
// declares `void`. The typechecker compares the full function signature of each
// impl method against the trait's required signature and rejects any mismatch.
