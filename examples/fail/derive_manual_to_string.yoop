import { Display } from "std/core/traits.yoop";

@derive(display)
type Clash implements Display {
  n: int32,
  function to_string(ref self): string {
    return "manual";
  }
}

function main(): int {
  return 0;
}
