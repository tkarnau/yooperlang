# Illegal: the impl of `dispose` returns int32 where the trait declares void.
# Same rule as the parameter case - the whole signature has to match.
5:6: returns int32, but "Disposable.dispose" declares void
