extern "C" from "stdio.h" {
    function printf(fmt: string, ...): int32;
}

task compute(x: int32): int32 {
    return x * x;
}

function main(): int32 {
    pooled h = compute(5);
    const v: int32 = wait h;
    printf(`h=${v}\n`);
    return 0;
}
