module shapes;

// Files inside one module have no dependency order, so these decls are
// processed AFTER the sibling above that already referenced them.

export enum Color { RED, GREEN, BLUE }

export union Bits {
  asInt: int32,
  asFloat: float32,
}
