extern "C" from "stdio.h" {
    function printf(fmt: string, ...): int32;
}

task compute(x: int32): int32 {
    return x * x;
}

function main(): int32 {
    joined d = compute(4);
    const v: int32 = wait d;
    printf(`d=${v}\n`);
    return 0;
}
